# 项目进度、完整 PRD 剩余任务与续开发交接

> 现场核对：2026-09-08，Asia/Shanghai（UTC+08:00）；文件生成时间：2026-09-08-181352+0800。  
> 仓库：`F:/AI_Movie_Things_202606/Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0/infinite-atelier-mine-0903`  
> 分支：`codex/wp-01-desktop-foundation`；HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`。  
> 本次交付范围：文档整理、Comet 恢复与文档独立核验；**未实施 WP-02**。  
> 配套提示词：`continue-wp02-codex-zcode-2026-09-08-181352+0800.md`，全文也位于本文第 9 节。  
> 当前产品结论：**WP-01 COMPLETE（Windows foundation 范围）；WP-02 尚未实现。**

## 1. 当前开发进度与证据边界

项目已有 Infinite Atelier 自由画布和多媒体前端，Windows/Wails/Go/SQLite/FileStore 底座已经完成。目标安全网关、持久任务、Drama 领域、三层 Agent、Workflow、Memory 和成片链路仍需开发。不能用工作包数量换算总体完成百分比，当前还不是安全桌面 MVP 或 Release Candidate。

| 层次 | 当前结论 | 仍不具备的完成证明 |
|---|---|---|
| 既有产品 | 画布、文本/图像/视频/音频/Group/Director、素材/提示词/历史/配置、MONOFORM 有旧实现 | 不代表安全、迁移、全节点回归或持久生成任务合格 |
| WP-00 | COMPLETE，基线与审计已完成 | 历史审计记录不等于风险修复 |
| WP-01 | Tasks 1–11 完成；AC-FOUND-001/002/003 Windows foundation 有历史 PASS | 不是其他平台、race、远端 CI 或整个 PRD 完成 |
| WP-02 | 用户提供的叙述记录计划已批准；未产生本包代码、migration、数据变更或验收结果 | 不能标 PARTIAL 或已实现；下一会话需实际实施 |
| WP-03～12 | 未实现目标工作包；旧功能可复用但不算包验收通过 | Job/迁移/Drama/Agent/Workflow/Memory/导出/硬化均待完成 |
| 发布 | 发布阻断尚未清零 | 不能以 exe 可启动宣布 RC |

### WP-01 已完成内容

| Task | 实现与历史证据 | 主要路径 |
|---|---|---|
| 1 | npm lockfile/平台包修复，后来本地安装和构建有验证记录 | web/package.json、web/package-lock.json |
| 2 | Wails v2.15.0、Go 1.25.0，嵌入现有前端与单实例 | main.go、app.go、go.mod、wails.json |
| 3 | App dirs、稳定 AppError/diagnostic ID、结构化日志/脱敏、关闭生命周期 | internal/domain/apperror、infrastructure/appdirs、logging |
| 4 | 唯一 modernc.org/sqlite v1.58.0，WAL/FK/busy timeout；历史 CGO=0 与跨平台测试包编译 | internal/infrastructure/database |
| 5 | 有界 migration runner，排序/校验和/事务/回滚/safe mode/pre-migration snapshot | database/migrate.go 等、ADR-0002 |
| 6 | SHA-256 流式 FileStore、MIME/magic、临时写入/原子提交/去重/路径防护/引用 | internal/application/files、infrastructure/filestore、database/files.go |
| 7 | 只读 HealthBinding.Get、core:event v1、安全启动失败返回、生成 bindings | internal/application/health、internal/desktop、web/src/wailsjs |
| 8 | React Query health、按能力加载桌面 client、browser 兼容、中英文/a11y 状态 | web/src/services/desktop、hooks、desktop-health-status |
| 9 | production clean build、隔离 native 启停/单实例/重启及五路由，历史双审 | wp01-execution-2026-09-07.md、153906 handoff |
| 10 | verify 两入口包含根 Go tests/vet/Wails、失败码与 .gitkeep 恢复；新增 Windows CI；README；独立双审 APPROVED | scripts/verify.ps1/sh、desktop-build.yml、README |
| 11 | final tests/vet/verify/Wails 与隔离 native smoke，Foundation 三 AC Windows PASS | STATUS 第 0 节、TRACEABILITY、ADR-0002 后续证据 |

正式 migration 仍只有 `schema_migrations`、`file_objects`、`file_references`。无 Provider/SecretReference/Job/Project/Episode/Script/Shot/Workflow/AgentRun/Memory 业务表；新 SQLite 底座尚未接管旧浏览器项目数据。当前 internal 模块清单支持该边界，不把目录骨架或旧 Canvas 当作目标业务实现。

### 权威来源与本次核验

事实顺序遵循 PRD → AGENTS → Architecture/Domain/Agent/Security/Acceptance → Roadmap → STATUS 已验证事实 → 代码测试 → 参考分析 → 推断。入口包括：

- `PRD.md`、`AGENTS.md`、`docs/ROADMAP.md`、架构/领域/Agent/安全/验收全套规格。
- `docs/implementation/STATUS.md` 第 0 节和 `handoff.md`（本次读取的 538 行根交接）。
- `docs/implementation/handoff-2026-09-08-153906+0800.md`、`wp01-execution-2026-09-07.md`、WP-01 plan。
- `docs/implementation/TRACEABILITY.md`、`docs/adr/0001-desktop-framework.md`、`0002-sqlite-driver-and-migrations.md`。
- 本次读取实际代码、migration、bindings、verify、CI、README 与包配置，独立事实核验发现的冲突已纳入下节。

| 检查类别 | 证据归属与真实结果 |
|---|---|
| git status/HEAD/staged | 本次核验：上述分支和 HEAD；索引为空；大量现有 tracked/untracked 内容 |
| git diff --check | 本次恢复后执行 exit 0；既有 LF/CRLF 与全局 ignore 权限提示。该命令不覆盖 untracked 新文档，交付物另做内容检查 |
| 文件与规格审阅 | 本次只读核对；不等于重跑所有产品验收 |
| 新交付文档内容检查 | 本次 PASS：19 FR、6 NFR、6 E2E、WP-02～12 全节与状态边界齐全；101 条待办；内嵌/独立完整提示词逐字一致；旧交接仍在 |
| Comet status/doctor | 本次先 exit 65，再按用户恢复授权隔离损坏原件、公共 repair/select，最终 doctor exit 0、healthy true、findings 空 |
| Go tests/vet、typecheck/build、两个 verify、Wails/native smoke | 引用历史 STATUS/执行证据，本次文档轮未重跑，不称本轮产品测试 PASS |
| 独立文档 Verify | 由 Comet 本轮新 candidate 流程记录；读取当前 verification.md 看最终结果，不能复用上午结果 |

历史 Task 11 smoke 使用自有 PID 5944，并隔离 APPDATA/LOCALAPPDATA/USERPROFILE/HOMEDRIVE/HOMEPATH/WEBVIEW2_USER_DATA_FOLDER，正常开窗/退出，读回 schema [(1,)]、WAL 和 130-byte 日志。日志敏感模式未命中是历史该测试日志的检查，不证明全仓库不存在密钥。Task 9 五路由不是全节点交互/全部旧项目迁移 E2E。

## 2. 已知矛盾、安全缺口与实施注意

1. **STATUS 顶部仍为 IN PROGRESS，而第 0 节已 WP-01 COMPLETE。**按最新实际实现与验收记录认定 Windows 包完成，WP-02 启动时最小纠正表头并建立新包基线；保留 WP-00 历史，不重复做 Task 10/11。
2. **旧根 handoff 部分 FR 编号映射错误。**例如 FR-020 是项目/原著、FR-030 是事件图谱、FR-040 是 Script，不是 SQLite/FileStore/Provider。第 3 节按当前 PRD 重新映射。TRACEABILITY 还缺 FR-030，存在陈旧 AppError/Binding 描述、部分非正式 AC 标签和格式问题；不能把旧表全部当作正确事实。
3. **WP-02“已批准”叙述与“待批准”标题矛盾。**保留为计划记录冲突；本次不实施。第 9 节提示词明确授权新会话只实施列出的 WP-02，使接手边界具体且无歧义。没有独立 WP-02 实施文件可据以声称已编码。
4. **ADR-0002 仍 Proposed。**后续已有 runner/Wails/SQLite 证据，成熟迁移工具与有界自有 runner 的历史差异有 ADR 讨论；不能为了对齐旧文字重写现有 runner或擅自标 Accepted。race、非 Windows native、性能和完整恢复证据仍有限。
5. **race 为 ENVIRONMENT FAIL。**32 位 MinGW 不能编译 amd64 CGO。普通 Go tests/CGO=0/交叉编译不能代替 race；Windows 原生验证也不能代替 macOS/Linux 打包与运行。
6. **锁文件旧失败和新证据分开。**WP-00 npm ci 失败后来已有局部修复、本地 Wails/verify 等记录；远端 GitHub Actions 未运行，干净机器安装仍需实际证据，不据旧 handoff 无条件重修依赖。
7. **遗留 key/script/网络风险仍真实存在。**前端配置/persistence 保留 apiKey，model-plugin 使用 new Function，config 普通备份路径可携带 raw 配置；浏览器 Provider 直连和任意 target Vite proxy 仍在。README 的 loopback 例子与 Wails watcher 不代表 package dev/start 的 0.0.0.0 已消失。
8. **多媒体 legacy 与 secure mode 边界必须显式。**WP-02 不立即完成全部旧调用迁移，但不得以自动 fallback 或 toggle 绕过安全保证，也不得据文本安全路径通过宣布全应用安全。WP-03/04 迁移，WP-12 删除或证明发布构建不可达。
9. **Secret 录入不能玩文字游戏。**PRD/SECURITY 禁 API Key 进入前端；React password input 临时持有 key 仍需检查严格契约，不能只因“不持久化”就宣称满足。实施时优先原生安全录入边界；无法满足已批准契约时提出具体产品/安全决策，其他工作继续。
10. **未提交现场不能只靠分支恢复。**新 clone/worktree 不包含大量 Go/规格/Bindings；本机用当前目录，跨机必须另准备审查过的源码包。勿传真实 secret、DB、用户素材、node_modules/build 作为默认续接材料。

## 3. PRD 全量功能映射

“部分”表示有相关基础或遗留功能，不表示已通过该 FR 的完整验收。

| PRD | 当前状态 | 剩余主责工作包 |
|---|---|---|
| FR-001 现有功能兼容 | 原有 UI 存在；迁移与完整回归未完成 | WP-04、WP-12；持续回归 |
| FR-010 桌面运行时与 Go Core | WP-01 Windows 底座完成；Worker、其他平台及发布硬化仍待接入 | WP-03/07、WP-12 与平台版本 |
| FR-020 项目、原著与章节导入 | 原有自由画布项目存在；Drama/原著导入未实现 | WP-04/05/06 |
| FR-030 章节事件图谱 | 未实现 | WP-05/06，消费端 WP-08/09/10 |
| FR-040 ScriptAgent 剧本流水线 | 未实现 | WP-07/08 |
| FR-050 资产圣经 | 通用素材能力存在；正式领域、版本和批准未实现 | WP-05/09/10 |
| FR-060 导演规划与 MONOFORM | 遗留预演存在；Shot 上下文和安全双向桥未实现 | WP-09；深度增强后续版本 |
| FR-070 分镜表、面板、分镜图 | 未实现 | WP-05/09 |
| FR-080 视频、音频、字幕与导出 | 有旧生成节点；持久镜头、字幕、时间线、FFmpeg 闭环未实现 | WP-03/11 |
| FR-090 Agent 中心 | 未实现 | WP-07，完善于 WP-08/09/10/12 |
| FR-100 Durable Workflow | 未实现 | WP-07，业务接入 WP-08～11 |
| FR-110 Supervisor 与质量中心 | 未实现 | WP-07/08/09/10/11 |
| FR-120 Persistent Memory | 未实现，聊天历史不等于 Memory | WP-07 Recent port、WP-10 核心；V1 增强 |
| FR-130 画布语义化 | 旧无类型连线与画布存在；领域投影/命令同步未实现 | WP-04/05/08/09/12 |
| FR-140 Provider Gateway 与模型策略 | 旧前端 Provider 存在；目标 Go 网关未实现 | WP-02/03；策略 WP-07～11；清退 WP-12 |
| FR-150 Persistent Job Manager | 旧内存轮询不满足要求 | WP-03，后续业务接入 |
| FR-160 素材存储、版本与血缘 | FileStore 和基础引用已实现；资产版本/血缘/缩略图/GC 未完成 | WP-03/04/05/09/12 |
| FR-170 备份、恢复与迁移 | 基础 DB 快照已实现；普通无密钥备份、旧项目迁移和恢复未实现 | WP-04/12；Secret 配合 WP-02 |
| FR-180 设置、日志、诊断与隐私 | 基础日志/脱敏/health 和旧主题语言存在；完整设置/诊断/隐私流程未实现 | WP-02/03/07/10/11/12 |

## 4. 全部剩余任务：当前优先 WP-02，后续按依赖授权

**WP-01 没有剩余的已规划 Task 10/11 实现任务。**仍需保留其 race/平台/ADR/远端 CI 等证据缺口，由相应后续范围收口；状态和映射小修列入 WP-02 启动，不因此把全部 WP-01 重新开放。

### WP-02 — Secret、网络安全与 Provider Gateway 基础

依赖：WP-01 Windows foundation。当前状态：计划已记录、未实现。目标是 Windows 原生 SecretStore、受控 Go HTTP 与第一个 OpenAI-compatible 文本生成/流式适配器。

包含：SecretStore、Provider 元数据与 SQLite reference、SSRF client、Registry、文本/health/audit、窄 binding、前端安全状态/迁移提示、普通 config/backup Secret 排除、扫描/测试/文档与独立审查。

不包含：图片/视频/音频完整迁移、持久 Job、Agent、Workflow、Memory、Drama、完整 backup/restore 重构、真实 Provider 调用。

#### 已批准计划的完整可执行清单（尚未实施）

- [ ] 修正 STATUS 的 WP-01 表头/正文矛盾，保留历史结果，记录 WP-02 开始基线与本次明确授权；不要重做已完成的 Task 10/11。
- [ ] 新增 Provider 领域类型、稳定安全错误分类和 Application ports/services。Domain 不依赖 Wails、SQLite、HTTP 或 OS API。
- [ ] 增加前向 SQLite migration：secret_references 与 Provider 配置元数据；DB 仅存 SecretRef/非敏感配置，不存 secret value，不修改 000001_foundation.sql。先 fixture/备份与失败回滚测试，再迁移。
- [ ] 实现 Windows Credential Manager native SecretStore，采用 advapi32 的 CredReadW/CredWriteW/CredDeleteW/CredFree（可通过已存在的 x/sys/windows 调用）；non-Windows 返回 unavailable。fake 仅测试使用；凭据测试只操作唯一测试前缀和自有测试条目，不枚举或清理用户凭据。
- [ ] 实现受控 Provider HTTP：scheme/URL/host/port/allowlist、DNS A/AAAA/IP/IPv4-mapped、redirect、TLS 复验，绑定已验证 IP 拨号以防 rebinding；限制 Header、环境代理、connect/TLS/header/idle/total timeout、响应大小。默认拒绝本地/私网，只有用户明确批准的精确本地 Provider 例外。
- [ ] 实现 trusted Provider Registry、OpenAI-compatible text generate/SSE stream/cancel、health check 和脱敏 audit。处理 Authorization、timeout、429、5xx、坏 JSON、流中断和取消；使用 httptest/可控 DNS 与 fake，不调用真实付费 Provider。
- [ ] 接入 app.go/main.go 与窄 Wails bindings：只提供配置、Secret 状态/设置/替换/删除、文本请求及取消。绝不暴露 Resolve、通用 fetch 或 arbitrary proxy。使用固定 Wails CLI 重新生成 bindings 并审计导出面，绝不手改生成文件。
- [ ] 前端新增 desktop Provider/Secret client 和安全 DTO；secure mode 禁用 query-key 初始化及动态 model script，迁移文本流、取消、错误和状态 UI，加入中英文迁移提示。旧多媒体仅保留为显式 legacy 兼容范围，不让安全失败自动回退旧脚本/旧 key/任意代理。
- [ ] 普通 config export/import 与 ZIP backup 去除 raw secrets，并提示 legacy 配置迁移；不修改用户原始配置/备份。secret value 不得在 Store/返回 DTO/log/Agent/普通备份中出现；旧配置导入不等于密钥已迁移或已从所有介质安全清除。
- [ ] 把动态执行和 Secret/static scans 接入 verify/CI；遗留例外必须精确、可审查且有 WP-03/04/12 清退归属，禁止全局 ignore。执行 Go/TS/Wails/security 检查，新增恶意 URL/redirect/取消/错误/导出 secret 排除等负面测试。
- [ ] 完成独立 spec review、修复/复审，再独立 quality review、修复/复审；将真实命令、AC 结果与限制写入 STATUS、TRACEABILITY、ADR/依赖记录。仅完成 WP-02 后停止，不进入 WP-03。

#### 模块、验收与收尾

- 预计 Go：internal/domain/provider；application/secrets、providers；infrastructure/secretstore、providerhttp、providers、database；desktop、app.go、main.go。
- 预计前端：services/desktop、配置 Store/channel UI、文本请求、config/backup 服务、i18n；bindings 按当前 wails.json 生成（当前 web/src/wailsjs）。
- 预计工程文档：verify scripts、CI、fixtures、STATUS、TRACEABILITY、ADR/依赖记录。fake 仅测试，生产 non-Windows unavailable。
- AC-FOUND-004：native/fake SecretStore，DB 仅 reference，Resolve 不出 Go，Binding/Store/log/普通备份无 secret。
- AC-FOUND-005 仅文本/安全子集：Authorization、timeout、429、5xx、坏 JSON、SSE/cancel、allowlist、redirect SSRF；完整多模态留 WP-03。
- AC-SEC-001：完整 SSRF corpus 与显式精确本地 Provider 例外，DNS pinning/重定向/TLS 负面测试。
- 最终命令：根 go test ./... -count=1、go vet ./...、format/static/security scans；web typecheck/build；可用两套 verify；Windows Wails production build；git diff --check；并发部分 race 在支持平台验证。不存在的 frontend test/lint 不称 PASS；环境失败明确标记。
- 独立 spec → 修复/复审 → quality → 修复/复审；每一批准有证据。完成后停止，不自动进入 WP-03。

### WP-03 — 持久任务与多模态 Provider

依赖 WP-02；验收 AC-FOUND-006、AC-MEDIA-001 Mock。

- [ ] GenerationJob/JobAttempt/ProviderRequest/JobDependency 表、状态机、幂等键、revision/lease、priority 与事件日志。
- [ ] SQLite 队列、scheduler、worker pool、Provider 并发/速率限制、进度、超时、指数退避、最大重试、依赖调度。
- [ ] 启动 recovery scanner；有 remote ID 优先查询，不重复提交未知计费请求；不可恢复安全失败，保留诊断。
- [ ] OpenAI-compatible Image/Edit、Gemini-compatible Text/Image、异步 Video submit/poll/fetch/cancel、基础 Audio/TTS；生产 Adapter 与测试 Mock 严格隔离。
- [ ] URL/Base64/Binary 结果进入 FileStore，限制大小/内容、临时文件校验、原子提交；下载未完成不得标完整成功，remote_only 有明确语义。
- [ ] 取消贯穿：停止后续产物提交，记录无法远端取消的 orphan candidate；安全保留此前已提交结果。
- [ ] Job Center 最小 UI，批量暂停/取消/恢复/仅重试失败；100 图片任务排队不冻结，供应商并发不超限，UI 进度合并。
- [ ] 现有前端生成接 Go Adapter，至少一条图像链路真实贯通；生产 flag 禁新增浏览器直连；重启、重复响应、失败不假成功测试。

### WP-04 — Legacy 迁移与画布后端适配

依赖 WP-03；验收 AC-LEGACY-001/002/003、AC-CANVAS-004、AC-BACKUP-001 基础、AC-E2E-001。

- [ ] 原创/脱敏 legacy fixtures：最小、全部节点、缺失媒体、畸形 metadata；版本识别、预检、迁移 manifest 与旧 ID mapping。
- [ ] Project/Canvas/Asset/File/History 基础 Repository；迁移前快照，Node/Edge/Viewport/Chat/History/媒体转换。
- [ ] 节点/连线数量、文字、媒体哈希、视口读回比对；未知插件节点、字段与 unsupported metadata 保留并报告，缺失文件显式告警。
- [ ] 幂等导入、重复提示、显式创建副本；临时空间验证、事务、失败回滚与清理；旧数据保留只读，不自动删除。
- [ ] Canvas persistence/repository adapter 接 Go；事实由后端持有，Zustand 仅 UI/交互；保留现有交互与视觉。
- [ ] 普通备份 v1：业务 snapshot、非敏感配置、素材、manifest/schema/checksum；不含 Secret；恢复原子性基础。
- [ ] 核心回归 E2E：新增/移动/删除、复制、框选/多选、缩放/平移、undo/redo、连接、小地图、裁剪/分割/mask、生成子节点、所有媒体预览。

### WP-05 — 短剧领域模型与工作室 UI

依赖 WP-04；验收领域不变量与 AC-CANVAS-001/002 基础子集。

- [ ] `free_canvas`/`drama` 项目；设置/规则/风格/模型策略、创建向导、语言/画幅/分辨率/集数/时长。
- [ ] SourceDocument/Version/Chapter；StoryEntity/Alias/Event/Relation/FactSource/Conflict/CharacterState；覆盖 PRD 所需人物、地点、组织、道具、时间与状态语义。
- [ ] Episode/Skeleton/Strategy/Script/Version/Scene/Dialogue/Shot；正确外键、顺序与来源关系。
- [ ] Asset/Version/File/Usage/Relation/Lineage 与 DirectorPlan/Storyboard/Item/Panel；领域类型覆盖角色、场景、服装、道具及 PRD 其他资产类型。
- [ ] Workflow/Stage/Review/Issue/UserGate 基础实体，为后续 Runtime 提供正式持久化基础。
- [ ] revision CAS、不可变内容版本、approved 唯一、锁定规则、软删除、stale 与影响传播基础；正式 SQL 迁移与负面测试。
- [ ] Domain Command/Query/Event 与 Binding；Canvas entity refs/Relation Registry、合法端点/required 引用/删除影响；旧无类型边可保留 generic。
- [ ] Studio 导航、项目总览及各业务页诚实空状态；不放静态假成功、不在本包接 LLM；数据完整性测试。

### WP-06 — 文档、章节与故事事实

依赖 WP-05；验收 AC-STORY-001/002。

- [ ] TXT/Markdown/DOCX/粘贴文本；编码检测、规范化、预览、SHA-256、不可变原始文件与文本版本、重复导入提示。
- [ ] 自动拆章、人工合并/拆分/确认；字符/段落范围定位、offset 不重叠；修改产生版本；10 万汉字处理与分块不冻结 UI。
- [ ] EventExtraction 输入输出 Schema、Application Service/受控 Provider；Runtime 未就绪时保持明确适配边界，Mock 仅用于测试。
- [ ] 人物/地点/组织/道具/事件/因果/时间/状态候选；接受/拒绝/编辑/锁定、合并别名、证据引用、冲突处理。
- [ ] Story Graph 列表与基础可视化；章节变更使下游事实待复核；为 Script/Supervisor 提供同一只读事实查询。
- [ ] 文档不可信边界、DOCX ZIP/XML/外部关系限制、大小/压缩比/取消/超时；恶意文档与跨项目 ID 测试。

### WP-07 — 三层 Agent、Skill、Workflow、Quality Gate

依赖 WP-05，按当前路线先完成 WP-06；验收 AC-AGENT-001～005。

- [ ] AgentRegistry/Spec、Skill Manifest/Loader/版本/hash、Schema/Tool 注册静态验证；项目覆盖有来源、限制与启用规则。
- [ ] 三个独立调用：Decision、Execution、Supervisor；Decision 无业务写，Execution 窄工具，Supervisor 只读且重新加载 DB。
- [ ] Tool 输入/输出 Schema、ACL、项目/剧集 scope、状态与 locked guard、超时、限长、审计；禁止 SQL/File/Network/Shell 原语。
- [ ] 结构化输出、一次 Schema repair、artifact ID/来源/版本 DB 读回；无效输出不半写，模型自述不能判成功。
- [ ] AgentRun/Message/ToolCall、精确 Skill/model/Provider/Workflow/Stage 关联、Token/费用/耗时/error；仅 reason summary，无私有思维链。
- [ ] Workflow/Stage 显式状态机、依赖、幂等、事务、输入输出快照、审计事件、暂停/取消/恢复；数据库控制合法动作。
- [ ] 用户 PASS/FIX/REDO/MANUAL_EDIT/SKIP/CANCEL 与原因记录；关键阶段门；max tool/time/token、最多自动修订 2 次、超限人工处理。
- [ ] Recent Memory port、召回早于写入当前消息的调用顺序；Script/Production Skill 骨架，不偷跑具体业务。
- [ ] Agent Center/Quality Gate 最小 UI；Skill 查看/新版本/回滚、启停与模型策略、调用详情/只读测试逐步接真实路径。
- [ ] deterministic Mock LLM Canary：Decision → Execution → Persist/Verify → Supervisor → User Gate；权限/幻觉/注入/修复失败/最大循环/取消/重启测试。

### WP-08 — ScriptAgent 三阶段

依赖 WP-06/07；验收 AC-SCRIPT-001/002/003。

- [ ] Script Decision Skill；故事骨架 execution/schema/tools 与独立 supervisor：命题、主支线、目标阻力代价、关键事件、集数、Hook/推进/反转/悬念及改编清单。
- [ ] 改编策略 execution/schema/tools/supervisor：受众、时长节奏、视角、场景/角色压缩、揭示顺序、可视化难点、成本风险红线。
- [ ] 结构化剧本 execution/schema/tools/supervisor：Episode/Scene/Dialogue/Shot 正式持久化；动作/对白/旁白、人物、时间/场景、前后状态、事件映射、时长与改编标记。
- [ ] 每阶段不可变版本、PASS/FIX/REDO/MANUAL_EDIT、锁定字段保护、Version Diff、旧版本保留和上下游追踪。
- [ ] Script UI、版本比较与审核定位、模型策略、Canvas 领域投影与双向命令更新。
- [ ] Canary 从原著/事件到 approved Script，验证修复缺失 Hook、重新审核、中断恢复及未满足依赖不得前进。

### WP-09 — ProductionAgent、资产、导演、分镜

依赖 WP-08 与 Job/Provider；验收 AC-ASSET-001/002、AC-BOARD-001/002/003、AC-E2E-002 生产部分。

- [ ] Production Decision Skill 与 director plan execution/schema/version：风格、摄影、构图、灯光色彩、运动、节奏、视线轴线、空间及模型限制。
- [ ] 资产缺口分析与用户清单/优先级确认；角色/场景/道具/服装/车辆/生物/风格参考/派生资产创建、导入、候选生成。
- [ ] 资产稳定 ID、属性/约束/负面约束、参考媒体、批准版本、适用集数/状态；角色多状态共存并按 Shot 引用。
- [ ] 候选比较、批准/淘汰/回滚、父子血缘、用途与影响分析；替换批准版本列出下游引用，旧版本保留。
- [ ] Storyboard Table Agent：Shot 编号/顺序、Scene、时长、景别机位运动、构图动作表情、资产引用、对白音效、首尾帧、视频运动、连续性与事件来源。
- [ ] Storyboard Supervisor 独立检查剧本覆盖、引用、镜头可执行性与连续性；问题定位 Shot/Panel/Asset，未通过或 required asset 缺失时阻止批量。
- [ ] Panel Agent、参考图/mask/参数、多候选图、Canonical Panel、Asset Registry 入库；批量 image jobs 暂停/取消/恢复/失败重试。
- [ ] Shot 重排正确更新顺序与关系；单 Shot 重做不改变其他 Shot；表格与画布双向同步、revision 冲突提示。
- [ ] MONOFORM typed bridge 基础：版本/source/origin/nonce/schema/大小限制；Shot 上下文传入，相机/预演图安全保存写回，子应用失败不破坏主数据。
- [ ] Production Canary：至少 2 角色、2 场景、12 Shots 到批准分镜图；每个结果可追溯 prompt/model/job/parent/source/agent/stage/review。

### WP-10 — Persistent Memory、一致性与质量中心

依赖 WP-07/08/09；验收 AC-MEM-001～005、AC-E2E-004/005。

- [ ] MemoryItem/SummarySource/EntityLink；episodic/semantic/procedural/artifact；role/agent/time/source provenance；与 Story Graph 分离。
- [ ] Scope Resolver：local-user/workspace/project/episode/agent/session；先过滤作用域再排序，跨项目泄露为零。
- [ ] Recent、Summary、Semantic recall、Pinned critical facts；先 recall 后写当前消息或明确排除 self-hit；阈值、TopK、去重、权重融合与 Token Budget。
- [ ] Float32 BLOB、VectorIndex 小规模精确搜索；Embedding provider 可换、版本切换/重建、关键词降级；无授权不外发项目文本。
- [ ] Deep Recall：summary 检索→threshold→rerank→source IDs→原始消息与实体版本→有界来源结果；明确历史问题才调用。
- [ ] Memory Inspector：查看/来源跳转/固定/编辑/降权/删除/重建；删源消息正确失效或更新摘要；locked 用户控制。
- [ ] 确定性一致性规则：角色/服装/伤势/道具所有权/场景/时间/引用/时长；语义 Supervisor 补充检查，区分证据来源。
- [ ] Quality Center：跨阶段问题、严重度、规则/版本、实体跳转、FIX/waiver/复审、stale 影响；统一执行与监督追踪。
- [ ] 召回与一致性评测：低相关空结果、隔离、自召回、来源删除、长期服装禁用设定恢复、局部修复及 token 上限。
- [ ] Embedding ADR；MVP Provider + 关键词降级即可，本地 ONNX/sqlite-vec 仅在对应许可、打包和产品范围获批后加入。

### WP-11 — 视频、音频、字幕、时间线、导出

依赖 WP-09/10；验收 AC-MEDIA-001/002/003、AC-E2E-003 媒体部分。

- [ ] Video Job UI 与受控 Adapter，支持分镜/首尾帧/参考资产/运动/时长/数量；远端状态、取消、下载、验证与恢复真实接入。
- [ ] Shot 多视频版本、预览/批准/替换；正式结果落本地并验证后成功；remote_only 与完整成功区分；重复响应不重复资产。
- [ ] 对白 TTS、旁白、音频导入与角色/台词关联；基础音量/起止时间、缺失台词检测。
- [ ] 字幕草稿、手动时间码/文字编辑、合法 SRT/VTT、外挂/烧录，镜头顺序和字幕同步。
- [ ] 基础 Timeline：有序镜头、音频字幕、clip 替换；不实现专业 NLE 全套。
- [ ] MediaEngine/FFmpeg ADR，探测/大小/时长/流限制、安全参数化、取消/超时、禁止 shell 字符串拼接。
- [ ] MP4 预览/最终质量，横/竖/自定义分辨率；剧本/分镜/字幕/导出 manifest 与版本信息。
- [ ] Final Supervisor：批准镜头/媒体/音频字幕/stale/waiver/时长/异常与资源元数据；人工质量门，导出可播放验证。
- [ ] 完整单集 E2E 与强制中断恢复；Mock 测试通过与真实供应商联调结果分开，未获授权不调用付费接口。

### WP-12 — 硬化、性能、打包、Release Candidate

依赖 WP-01～11；验收全部当前发布范围 AC，清零发布阻断。

- [ ] 两套完整 verify：format/vet/tests、lockfile install/typecheck/component/build、schema/skill、security、license/SBOM；失败/skip 明确。
- [ ] Unit/Application/Repository/Provider/Agent/Workflow/Memory/Frontend/E2E/Security 全层测试；原创 Canary 与恶意 fixtures 有来源许可证说明。
- [ ] 性能：1000 nodes/2000 edges、1 万 asset/Memory、500 job history、大文件/ZIP；项目打开 P95 <3s、普通 DB 命令 P95 <100ms、进度 UI ≤10 次/秒。
- [ ] Canvas 按 viewport/selection/drag/connection/history/projection/generation/persistence 拆分；normalized map、局部 selector、节点与边裁剪、分页/虚拟化、避免无界深拷贝。
- [ ] Backup/Restore 完整：schema/manifest、hash、snapshot、迁移临时副本、Zip Slip/Bomb/设备名/symlink/重复规范路径限制、原子提交与失败项目不变。
- [ ] 从前序已发布 schema 升级；迁移前快照、校验和、完整性、降级拒绝/前向修复、恢复模式；禁止改写已发布 migration。
- [ ] FileStore 全资产接入、缩略图、引用计数/删除保护、GC 预览和取消、缓存/临时 TTL、安全完整性检查；GC 不删其他项目引用和批准资产。
- [ ] 设置闭环：数据目录、缓存、Worker、timeout、模型/Memory 策略、日志级别、FFmpeg、自动备份、主题/语言；数据目录变更需安全迁移策略。
- [ ] 日志轮转/保留上限、Trace/Run/Job/Workflow 关联、原始响应 TTL、diagnostics 清单与脱敏预览/取消/导出；默认无遥测与自动正文上传。
- [ ] Provider 隐私/数据外发展示与授权、fallback 不绕过授权；缓存清理/记忆删除/项目导出与永久删除控制。
- [ ] 删除或使发布构建不可达：任意 JS 模型脚本、前端原始 key、browser direct Provider、任意 target proxy、未校验 iframe 与新事实 localForage 路径；所有入口有回归证据。
- [ ] Secret scan、npm/Go 依赖漏洞检查、MIT/版权、完整 THIRD_PARTY_NOTICES/SBOM、构建 artifact 哈希；测试和 CI 不打印密钥。
- [ ] Windows clean VM：无 Node 的安装/启动/关闭/重启、WebView prerequisites、安装/升级/恢复/卸载数据保护；installer/signing 策略和实际验证分开。
- [ ] 中文首发/英文结构、键盘/焦点/非颜色状态、Markdown/SVG/CSP/外链/剪贴板/拖放/WebView 权限与无障碍回归。
- [ ] 用户文档、故障恢复说明、release checklist；六项 E2E 全部记录证据；未通过发布阻断不标 RC。
- [ ] 加密敏感备份作为独立能力，仅在 ADR/版本范围明确后实现密码 KDF+AEAD、预览、错误密码与明文临时清理；不能混入普通备份默认按钮。

## 5. Roadmap 容易遗漏的 PRD 子项及归属建议

下表属于任务规划建议，正式写入工作包计划时需按事实优先级处理，不扩大本次文档工作或已批准的 WP-02。

| 容易遗漏项 | 归属建议与验收重点 |
|---|---|
| 声明式 HTTP Manifest Adapter | WP-02 契约/安全，WP-03 能力映射，WP-12 清退前完整验证：endpoint/method/header/body/status/JSONPath/poll/allowlist，禁止脚本与任意网络 |
| Embedding Adapter/关键词降级 | WP-10；不能只写向量表而无可用查询与降级路径 |
| Provider Webhook（若支持） | WP-03/11 按适配器能力处理；本地桌面不能为此默认开放公网 listener，不支持则明确 polling 能力 |
| FR-090 Skill 新版本/回滚/只读测试/诊断导出 | WP-07 管理基础，WP-12 与诊断闭环；仅注册 Skill 不算 Agent 中心全部完成 |
| FR-150 非生成任务 | Job 基础不能只覆盖图片：Embedding/下载/缩略图/导入/导出/摘要/迁移等按各业务包接入，保留取消和恢复策略 |
| FR-160 资产血缘、引用与 GC | WP-01 仅物理对象基础；完整 parent/job/prompt/model/parameters/source/review/usage 在 WP-03/05/09，GC 与限额在 WP-12 |
| FR-180 全局设置与隐私/诊断 | 横跨各包；由 WP-12 最终逐项收口，不能因没有专门 WP 而漏做 |
| 旧项目 Prompt/History/未知节点 | WP-04 fixtures 与 read-back；旧 UI 可打开不能代替迁移完整性 |
| stale/waiver 与最终导出 | WP-05 不变量、WP-09/10 传播、WP-11 final gate/manifest；不能静默采用失效版本 |

## 6. 非功能要求和最终成功判据

| PRD NFR | 正确含义 | 验收归属 |
|---|---|---|
| NFR-001 | 性能 | WP-06 大文本、WP-03 queue/UI、WP-12 Canvas/asset/memory/DB P95 |
| NFR-002 | 可靠性 | WP-01 migration/snapshot、WP-03 job、WP-04 import、WP-07 workflow、WP-12 recovery |
| NFR-003 | 安全 | 各信任边界负面测试，WP-12 全量安全语料和发布门 |
| NFR-004 | 可维护性 | 分层、明确 port、Schema 版本、稳定错误、大组件拆分、不引入大型 Agent 框架 |
| NFR-005 | 测试 | Go/Repository/Mock Provider/状态机/Memory/React/Playwright/迁移/安全/性能 |
| NFR-006 | 可访问性与国际化 | 每个 UI 包实现，WP-12 整体键盘/焦点/中英文验证 |

最终必须完成的六个 E2E：

1. **AC-E2E-001**：旧项目完整导入，全部媒体预览，新图片任务，新备份不含 key。
2. **AC-E2E-002**：≥3 章、≥3 万汉字故事，3 集规划，第 1 集剧本通过监督，≥2 角色/2 场景/12 镜头，生成分镜并全链路可追溯。
3. **AC-E2E-003**：视频和 storyboard workflow 运行中强制关闭，重启显示真实阶段，恢复或安全失败，无重复资产。
4. **AC-E2E-004**：故意服装错引，监督定位，FIX 新版本，旧版保留，通过后才能推进。
5. **AC-E2E-005**：早期禁用红色服装，经大量后续消息后 Deep Recall 回到原始来源及资产，不串项目。
6. **AC-E2E-006**：backup/log/frontend 无 key，恶意 URL/ZIP 被阻断，Supervisor 无写工具。

PRD 指标同样需要受控评测而非猜测：导入到骨架成功率 ≥90%、恢复 ≥95%、连续性问题可定位 ≥90%、输出首次 Schema ≥90%/一次 repair 后 ≥98%、监督问题实体定位 ≥95%；可迁移节点/边不丢失 100%、任务与实际结果一致 100%、重复幂等产物 0、密钥泄露 0。当前未测得这些整体指标。

## 7. MVP 之后的完整规划边界

不能把这里所有未来项塞入当前工作包，也不能用未来版本名取消 FR 明确要求的基础能力。

| 版本/条件 | 后续待办 |
|---|---|
| MVP/v0.5，至 WP-11 + WP-12 发布硬化 | 完成原著→事件→剧本→资产→导演→分镜→视频→基础 TTS/字幕→单集 MP4，并具备安全/恢复/记忆/审核/迁移 |
| v1.0 | 许可与打包核验后的本地多语言 ONNX、可选 sqlite-vec、层级摘要/增强记忆中心和召回评测；更强事件图谱可视化、MONOFORM 深度集成、资产一致性、视频首尾帧/批量增强；音效建议/生成、背景音乐导入、简单混音、多角色声线、更完整时间线；Windows 稳定安装升级；PDF 导入增强 |
| v1.5 | macOS/Linux 打包、Workflow 模板、模型路由与成本预算、资产批量替换/影响分析增强、更大画布性能、外部剪辑格式研究与适配 |
| v2.0，当前范围外 | 可选团队/云同步/多租户 API/PostgreSQL-pgvector/Worker 集群/S3-MinIO/订阅 Credits 审计；尚不具备批准详细工作包 |

需要在对应包开始前记录的规格差异：

- FR-120 的“必要规则”要求层级摘要与管理操作，而 MVP/V1 段落将部分 UI/层级摘要放 V1；WP-10 明确包含 Inspector。至少按 WP-10 完成管理/来源/删除基础，层级深度通过 ADR/产品边界明确，不静默延期。
- FR-060/080 的基础 MONOFORM、首尾帧、MP4 已在 FR/MVP/当前 Roadmap 中出现；版本规划中的“深度/更完整”不能作为不实现基础闭环的理由。
- ACCEPTANCE 将 macOS/Linux 必须验证放在 v1，PRD 版本规划把打包列在 v1.5。以 PRD 优先，分别记录验证和打包范围；若影响承诺需产品决策。
- PRD、Domain、Architecture 中 Workflow/Stage/Job 状态名有差异（如 awaiting_remote/waiting_remote、recovering/verifying、executed/execution_succeeded）。WP-03/07 应以 PRD 行为为准建立统一规范或显式映射，写 ADR/Schema/测试，不凭模型文字推断。
- PRD 的资产类型与关系集合比部分 Domain 示例更广，WP-05 需逐一覆盖或明确映射，不能将示例枚举误当全部需求。
- 加密敏感备份在 FR-170 有要求、Roadmap 为 ADR 条件项；作为独立待办保留，正式发布时间归属需明确，不偷塞、不漏记。

## 8. Git 现场、Comet 恢复与交接使用

当前 tracked dirty：.github/workflows/desktop-build.yml、.gitignore、README.md、web/package-lock.json、web/package.json、user-status-actions.tsx、en-US.ts、zh-CN.ts。根规格/多份 prompts、docs、.agents/.codex/.comet/.zcode、Go/Wails/internal、verify scripts、web/dist、health files 和 web/src/wailsjs 等为已有 untracked。清单必须由新会话重新运行 git status --short --branch 获取完整当前结果，不能仅以本段作删除/提交白名单。

本次只新增本 handoff、配套提示词、必要恢复备份/记录并更新 Comet 正式 brief/spec 和 Runtime 工件。没有产品代码改动，没有迁移/业务备份、真实 Provider、commit/push/stash/reset/clean。未输出实际 secret 值；本次不是全仓库 secret audit。

Comet 恢复记录：`docs/implementation/comet-recovery-2026-09-08-180700+0800/RECOVERY.md`，原件哈希清单位于同目录。损坏的旧 change 原样移入 quarantined-original 子目录；4 文件 SHA-256 前后一致，没有手补 pass。
公共 doctor --repair 随后成功修复有效 change 的 migration/local-state；公共 select 清除 stale selection；doctor healthy true、findings 空。
尝试新建当前目录 change 被 Runtime 拒绝（同目录已存在 active）。按用户已确认当前目录和交接范围，现有 wp-01-session-handoff-20260908 通过公共 next return-to-build，再由 drift 检测返回 Shape，用户确认后的新 goal cycle 2 从 pending 验收重新 Build。历史上午 Task 7 pass 只保留在 history，不代表本次 candidate。
最新 Runtime 结果以公共 status 和 `docs/comet/changes/wp-01-session-handoff-20260908/verification.md` 为准；通用 skill bridge 的 pass 如需用户确认，应保留 await-user，不能把“Comet 可用”写成“已归档”。新会话不要盲目恢复该文档流程为 WP-02 实现状态。

使用方法：在本机新 Codex 或 zcode 打开本仓库当前目录，复制配套提示词全文。本文不自动创建新任务。只有用户实际向新会话提交提示词才是对新会话的 WP-02 实施授权。本会话不提前实施。跨机先安全迁移未提交代码，不可仅复制 handoff。

## 9. 完整续开发提示词（Codex / zcode 通用）

以下代码块与根目录 `continue-wp02-codex-zcode-2026-09-08-181352+0800.md` 全文一致，可整段复制：

```text
你是本仓库的资深 Go、React/TypeScript、Wails、SQLite 与安全工程师。请在当前项目继续开发，先核对现场，再执行且仅执行 WP-02 — Secret、网络安全与 Provider Gateway 基础。

工作目录：
F:/AI_Movie_Things_202606/Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0/infinite-atelier-mine-0903
最新交接：
F:/AI_Movie_Things_202606/Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0/infinite-atelier-mine-0903/handoff-2026-09-08-181352+0800.md

【明确授权与边界】
我通过提交本提示词明确授权你在当前目录、当前分支实施 WP-02 的下列计划，包括必要的代码、测试、前向 migration 文件、生成 bindings、文档和独立审查。不要只返回计划或重复询问是否启动已批准范围；完成可执行的授权工作。不要实施 WP-03 及之后，不自动 commit/push/stash/reset/clean，不访问真实付费 Provider，不修改真实用户密钥/数据库/媒体。
先核对 WP-01 完成事实；若发现与证据相冲突的新缺陷，记录真实阻断并修复当前包必要前置问题，不能编造通过。
本提示词不是对额外产品范围、真实凭据迁移、真实业务数据操作、破坏性操作或外部付费调用的授权。遇到真正未决的安全/产品选择，提出具体问题，同时继续不依赖该选择的工作。

【必须保留的现场】
预期分支 codex/wp-01-desktop-foundation，HEAD a243891455ec17687dd54b5ac90d3bd64478a1a1；开始必须重新核对，不能硬切回此值。
大量规格、docs、Go/Wails/internal、bindings 为未提交/未跟踪文件；只 clone 或新 worktree 会丢失现场。记录 git status --short --branch、git rev-parse HEAD、git diff --cached --name-status、git diff --stat，保护全部既有 tracked/untracked。不要删除未知文件，不覆盖旧 handoff，不修改 Git 历史。不要同时让另一个会话写相同目录。
包管理器 npm，尊重 web/package-lock.json 与独立 MONOFORM lockfile；版本以当前 go.mod/wails.json/package 为准（已知 Go 1.25.0、Wails 2.15.0、modernc.org/sqlite 1.58.0）。不照抄旧 Go 1.24 或错误 bindings 路径示例。

【必读与事实优先级】
先读最新 handoff 了解入口，随后在任何修改前按 AGENTS 指定顺序完整读：
1 PRD.md
2 AGENTS.md（并检查作用目录下其他适用 AGENTS）
3 docs/implementation/STATUS.md
4 docs/ROADMAP.md 的 WP-02 及依赖
5 docs/ARCHITECTURE.md
6 docs/DOMAIN_MODEL.md
7 docs/AGENT_CONTRACTS.md
8 docs/SECURITY.md
9 docs/ACCEPTANCE.md
10 docs/reference/INTEGRATION_ANALYSIS.md
11 docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md
12 README、package/config/CI、实际代码和测试。
再读根 handoff.md、docs/adr/0001-desktop-framework.md、docs/adr/0002-sqlite-driver-and-migrations.md、TRACEABILITY、WP-01 plan/执行证据，按需查看旧时间戳交接。
PRD 必须项优先于 AGENTS、Architecture/Domain/Agent/Security/Acceptance、Roadmap、STATUS 已验证事实、代码测试、参考分析、推断。冲突须明确记录，不静默删改历史。

【恢复事实】
WP-01 Tasks 1–11 已在 Windows foundation 范围完成，AC-FOUND-001/002/003 有历史 PASS；Task 10 verify/Windows CI/README 已实现，Task 11 final checks/native smoke 已记录，不能按旧提示词从 Task 7/9/10 重做。
STATUS 顶部 IN PROGRESS 与第 0 节 COMPLETE 矛盾，需最小修正。TRACEABILITY 有缺 FR-030、陈旧 AppError/Binding 描述和 AC 标签问题；按当前 PRD 校正当前包必需映射，其他缺口记录。
WP-02 只有计划，没有 Provider/Secret 代码或 migration；正式 foundation migration 目前只有 schema_migrations/file_objects/file_references。
ADR-0002 仍 Proposed，race 是 32 位 MinGW 不能编译 amd64 CGO 的环境失败；不能自动标 Accepted 或 race PASS。Windows 原生证据不证明 macOS/Linux；本地 CI 配置不证明远端 GitHub Actions 已跑。
旧 npm ci 失败是历史；后来有锁文件修复与本地验证，不凭旧 handoff 再次重修依赖。重新跑基线记录当下结果。
现有 legacy raw API key、new Function、browser direct Provider、任意 target Vite proxy、dev/start 0.0.0.0、普通含 config 备份仍是风险；现有 UI 不等于安全 Gateway 或 Drama 实现。

【WP-02 实施计划】
1. 修正 STATUS 的 WP-01 表头/正文矛盾，保留历史结果，记录 WP-02 开始基线与本次明确授权；不要重做已完成的 Task 10/11。
2. 新增 Provider 领域类型、稳定安全错误分类和 Application ports/services。Domain 不依赖 Wails、SQLite、HTTP 或 OS API。
3. 增加前向 SQLite migration：secret_references 与 Provider 配置元数据；DB 仅存 SecretRef/非敏感配置，不存 secret value，不修改 000001_foundation.sql。先 fixture/备份与失败回滚测试，再迁移。
4. 实现 Windows Credential Manager native SecretStore，采用 advapi32 的 CredReadW/CredWriteW/CredDeleteW/CredFree（可通过已存在的 x/sys/windows 调用）；non-Windows 返回 unavailable。fake 仅测试使用；凭据测试只操作唯一测试前缀和自有测试条目，不枚举或清理用户凭据。
5. 实现受控 Provider HTTP：scheme/URL/host/port/allowlist、DNS A/AAAA/IP/IPv4-mapped、redirect、TLS 复验，绑定已验证 IP 拨号以防 rebinding；限制 Header、环境代理、connect/TLS/header/idle/total timeout、响应大小。默认拒绝本地/私网，只有用户明确批准的精确本地 Provider 例外。
6. 实现 trusted Provider Registry、OpenAI-compatible text generate/SSE stream/cancel、health check 和脱敏 audit。处理 Authorization、timeout、429、5xx、坏 JSON、流中断和取消；使用 httptest/可控 DNS 与 fake，不调用真实付费 Provider。
7. 接入 app.go/main.go 与窄 Wails bindings：只提供配置、Secret 状态/设置/替换/删除、文本请求及取消。绝不暴露 Resolve、通用 fetch 或 arbitrary proxy。使用固定 Wails CLI 重新生成 bindings 并审计导出面，绝不手改生成文件。
8. 前端新增 desktop Provider/Secret client 和安全 DTO；secure mode 禁用 query-key 初始化及动态 model script，迁移文本流、取消、错误和状态 UI，加入中英文迁移提示。旧多媒体仅保留为显式 legacy 兼容范围，不让安全失败自动回退旧脚本/旧 key/任意代理。
9. 普通 config export/import 与 ZIP backup 去除 raw secrets，并提示 legacy 配置迁移；不修改用户原始配置/备份。secret value 不得在 Store/返回 DTO/log/Agent/普通备份中出现；旧配置导入不等于密钥已迁移或已从所有介质安全清除。
10. 把动态执行和 Secret/static scans 接入 verify/CI；遗留例外必须精确、可审查且有 WP-03/04/12 清退归属，禁止全局 ignore。执行 Go/TS/Wails/security 检查，新增恶意 URL/redirect/取消/错误/导出 secret 排除等负面测试。
11. 完成独立 spec review、修复/复审，再独立 quality review、修复/复审；将真实命令、AC 结果与限制写入 STATUS、TRACEABILITY、ADR/依赖记录。仅完成 WP-02 后停止，不进入 WP-03。

【预计模块】
internal/domain/provider/；internal/application/secrets/、providers/；internal/infrastructure/secretstore/、providerhttp/、providers/、database/；internal/desktop/、app.go、main.go；web/src/services/desktop/、配置 store/channel UI/文本请求/config/backup/i18n；verify scripts、CI、STATUS/TRACEABILITY/ADR、testdata。
依实际契约选择最小文件拆分，避免 God package/component。先领域/ports/测试再接 UI。Database 是事实来源，Zustand 是 UI 状态；单进程本地桌面，不引入微服务或大型框架。

【不可弱化的安全边界】
Windows Credential Manager 是已选实现；无 plaintext fallback，不把 Secret Resolve 提供前端。测试 fake 不进入生产。密钥不能进入前端持久状态、Binding 返回、日志、Agent、普通备份；native Resolve 仅 Go 内部受控消费。
PRD/SECURITY 对“API Key 不进入前端”的要求必须落实到录入方案。不要仅因 React password 字段不持久化就声称完全符合；先核对批准契约，优先满足原生安全录入边界。如必须让 WebView 短暂持有明文且没有明确批准，提出具体边界决策，不静默放宽要求；其他 SecretStore/HTTP 工作可继续。
完整 legacy 多媒体迁移、Job、Agent、Workflow、Memory、Drama、完整 backup/restore 重构不在 WP-02。本包需要普通导出/备份排除 raw secrets 的窄改动。secure mode 与剩余 legacy 范围明确分开，不提供安全错误自动回退，不宣称全应用已满足 PRD 发布门。
保持 TLS 校验；所有 DNS 与重定向复核；精确本地 Provider 例外必须用户明确授权。禁止通用 fetch/任意代理绑定、eval/new Function 新路径或为安全扫描增加全局豁免。仅记录 safe message/diagnostic ID，不打印 Header/key/完整 URL 查询参数。
前向 migration 不重写 000001；fixture/temp DB 验证与备份先行，不擅自迁移用户真实数据。凭据测试仅清理自有随机前缀条目。
保留 MIT/版权；Toonflow clean-room，不复制其源码/Prompt/Skill/品牌/素材；新增依赖核验许可证、维护状态并记录。

【执行与验证】
先记录当前可运行基线：根 go test ./... -count=1、go vet ./...；web npm.cmd run typecheck 和 build；检查 verify 脚本及生成物边界。环境/权限失败记为真实 FAIL/ENVIRONMENT FAIL，按可用授权机制处理后再重跑，不虚构成功。
小步实现，每个逻辑单元运行有意义的正反测试。使用 httptest、可控 DNS、确定性 fake、临时目录、唯一自有 Credential 测试条目；CI 无真实模型。
重点验收：
- AC-FOUND-004：native/fake SecretStore；DB 仅 SecretRef；Binding/Store/log/普通备份无 secret；unavailable fail closed。
- AC-FOUND-005 文本/安全子集：Go Authorization、超时、429、5xx、坏 JSON、SSE、取消、allowlist、redirect SSRF；不冒充图像/视频全子项通过。
- AC-SEC-001：完整 SSRF corpus 拒绝与显式精确本地 Provider 例外、DNS pinning、重定向/TLS。
最终执行根 go test ./... -count=1、go vet ./...、Go 格式/静态与 security scans、frontend typecheck/build、两套可运行 verify scripts、固定 Wails Windows production build、git diff --check；有并发场景在支持环境跑 race，宿主不支持则如实记录，不能拿 CGO=0 代替。
Bindings 用 Wails 生成到实际配置目录（当前 web/src/wailsjs），审计所有导出。构建可能清空 web/dist，保持现有 .gitkeep 约定；不要删除用户文件/素材。需要 native smoke 时隔离全部 app/profile/WebView 路径，只关闭本次自有进程。
前端 test/lint 若尚不存在不得记 PASS；在本包为安全边界选择必要测试，不借机大改无关格式。

【技能与独立审查】
若环境提供，使用 Ponytail 和 Comet；其路径在本机为：
C:/Users/Administrator/.codex/plugins/cache/ponytail/ponytail/4.9.0/skills/ponytail/SKILL.md
C:/Users/Administrator/.agents/skills/comet/SKILL.md
先读取实际 skill，公共 comet workflow resolve/status/doctor 读取最新 Runtime。交接 Comet change 是文档流程，不是 WP-02 实现完成证明；不要复用旧 pass、手写状态或盲目归档。已有 active change 或流程阻断按 Runtime 和用户明确决策处理，不擅自隔离更多记录。
按实现者 → 独立只读 spec reviewer → 修复/复审 → 独立只读 quality reviewer → 修复/复审推进。可使用真正独立子代理，避免并行写同一文件。若 zcode 缺某工具，用等价 shell/file/test 能力；无法独立审查时完成其余授权工作后明确保留待独立审查，不用自己换角色冒充两个批准。

【包末交付】
同步 STATUS 当前结果、TRACEABILITY 的真实 AC/FR、相关 ADR/Schema/Binding/测试/依赖记录；保留历史记录并区分新增证据。输出 AGENTS 要求的工作包、状态、已完成、修改文件、验证、验收、未完成风险、Git/数据安全、下一步报告。
WP-02 所有必需验收和独立审查真实满足才标 COMPLETE；否则 PARTIAL/BLOCKED 并说明具体缺口。停止，不自动进入 WP-03。
```
