# Infinite Atelier Drama Studio 开发交接

> 交接时间（Asia/Shanghai）：2026-09-08T15:12:49+08:00  
> 仓库：`F:\AI_Movie_Things_202606\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\infinite-atelier-mine-0903`  
> 分支：`codex/wp-01-desktop-foundation`  
> HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`  
> 当前工作包：**WP-01 — Wails v2、Go Core、SQLite 与文件底座**  
> 当前状态：**PARTIAL / BLOCKED**  
> 精确恢复点：**WP-01 Task 9 已实现并完成部分隔离生产验证，但 `/assets`、`/canvas`、`/director`、`/config` 缺少可信的直接原生 Wails 渲染证据；因此 Task 9 未获最终独立双审批准，Task 10 与 Task 11 不得开始。**

## 1. 一句话结论

WP-00 已完成；WP-01 Task 1–8 已通过 implementer → 独立 spec reviewer → 独立 quality reviewer 的串行门禁。Task 9 已完成生产构建、首页原生渲染、单实例、关闭/重启、数据库迁移持久化、日志脱敏和隔离用户目录验证，但其计划要求的四个非首页路由尚无可信原生渲染证据。此阻塞是宿主 WebView 自动化/可观测性限制，不是已确认的产品代码缺陷；不得以源码路由注册、盲点点击或测试专用路径代替原生证据。

## 2. 当前 Git 与数据安全现场

### 实时 Git 快照

下列命令在本交接生成前执行：

```text
git status --short --branch
git rev-parse HEAD
git diff --check
```

结果：

- branch：`codex/wp-01-desktop-foundation`
- HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`
- staged diff：未发现 staged 变更。
- `git diff --check`：PASS；仅出现既有 LF/CRLF 警告，无 diff whitespace error。
- tracked modified：`.gitignore`、`web/package-lock.json`、`web/package.json`、`web/src/components/layout/user-status-actions.tsx`、`web/src/i18n/locales/en-US.ts`、`web/src/i18n/locales/zh-CN.ts`。
- untracked 内容包括但不限于：规格文件、`docs/`、Go/Wails 产品文件、`internal/`、`scripts/`、`build/`、`web/dist/`、Task 8 前端文件、生成的 `web/src/wailsjs/`、`.agents/`、`.codex/`、`.comet/`。**所有 tracked/untracked 文件都必须视为需保护的现有现场，不能因为未跟踪而删除。**

### 安全边界与已执行动作

- 未执行 commit、push、stash、reset 或 clean。
- 未修改真实数据库、真实 AppData/LocalAppData、浏览器/WebView profile、密钥或用户媒体。
- 未访问真实 Provider 或付费 API。
- Task 9 仅使用调用进程专属的系统临时目录重定向 `APPDATA`、`LOCALAPPDATA`、`USERPROFILE` 和 `WEBVIEW2_USER_DATA_FOLDER`；全部临时数据库、文件和日志均在验证后检查并删除。
- `web/dist/.gitkeep` 是必须保留的零字节 embed-root 占位文件；构建会删除它，恢复后其 SHA-256 必须为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`。
- `build/bin/` 已被 `.gitignore` 忽略；Wails 可复现二进制不得提交。
- 已核对并移除两个确认属于自动化残留、无引用的文件：根目录 `NUL`（零字节重定向残留）和 `web/package.json.md5`（仅为当前 package 文件的 MD5 sidecar）。未删除用户数据。

## 3. 必读事实源与历史证据

任何后续代码修改前，必须严格按 `AGENTS.md` 顺序完整阅读：

1. `PRD.md`
2. `AGENTS.md`
3. `docs/implementation/STATUS.md`
4. `docs/ROADMAP.md` 中当前 WP-01
5. `docs/ARCHITECTURE.md`
6. `docs/DOMAIN_MODEL.md`
7. `docs/AGENT_CONTRACTS.md`
8. `docs/SECURITY.md`
9. `docs/ACCEPTANCE.md`
10. `docs/reference/INTEGRATION_ANALYSIS.md`
11. `docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md`
12. README、package、配置、CI、当前相关代码与测试。

继续 WP-01 还必须阅读：

- `docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md`
- `docs/implementation/wp01-execution-2026-09-07.md`
- 本文件及较早 handoff（仅作历史证据）：
  - `docs/implementation/handoff-2026-09-05-163956+0800.md`
  - `docs/implementation/handoff-2026-09-07-161115+0800.md`
  - `docs/implementation/handoff-2026-09-08-103244+0800.md`

文档漂移纠正：

- 旧 handoff 中“Task 7 中途、`web/src/wailsjs` 不存在”的说法已经过时。生成 binding、Task 7 和 Task 8 审核均已完成。
- `docs/implementation/STATUS.md` 是当前工作包状态入口；其中 Task 9 非首页路由 BLOCKED 的结论为当前事实。
- Wails v2.15.0 与 `modernc.org/sqlite v1.58.0` 要求 Go 1.25；计划中 Go 1.24 示例已过时。
- `wails.json` 的 `wailsjsdir` 为 `web/src`，生成器产物实际位于 `web/src/wailsjs/`，不得手工编辑。
- `ADR-0002` 仍为 Proposed；不要因已跑通局部 SQLite/Wails 证据而提前标为 Accepted。
- 最终 traceability 必须使用当前 PRD 标题：FR-010 桌面运行时与 Go Core、FR-020 项目/原著/章节导入、FR-160 素材存储/版本/血缘、FR-180 设置/日志/诊断/隐私。

## 4. 已完成的开发进度

### WP-00 — COMPLETE

完成仓库审计、可重复基线、路由/存储/Provider/动态脚本风险清单、traceability、初版 ADR 与 Windows/POSIX verify 脚本。WP-00 的历史结论保留，不等同于后续 WP 的完成。

### WP-01 Task 1–6 — APPROVED

1. **Task 1：npm lock 修复。** 已通过独立规格与质量审查。
2. **Task 2：Wails/Go 最小壳。** 固定 Wails v2.15.0、Go 1.25、正确生成目录和单实例组成；已批准。
3. **Task 3：Go Core 基础。** AppError、应用目录、结构化脱敏日志、生命周期，以及普通引号内敏感赋值的 TDD 修复；已批准。
4. **Task 4：SQLite 驱动。** `modernc.org/sqlite v1.58.0` 是唯一直接驱动；无 ORM、无第二 SQLite driver。完成 WAL/FK/busy timeout、驱动契约、CGO-disabled 与 Windows/Linux/macOS 测试编译证据；已批准。
5. **Task 5：迁移与安全启动。** migration checksum/排序/事务、pre-migration `VACUUM INTO` snapshot、safe mode、integrity fail-closed、close checkpoint；已批准。
6. **Task 6：FileStore。** 内容寻址、SHA-256、MIME/magic、原子提交、去重、文件名/路径防护、引用/外键和缺失文件诊断；已批准。

### WP-01 Task 7 — APPROVED

- `internal/application/health` 提供安全的 Snapshot/Probe 健康服务。
- `internal/desktop/health_binding.go` 的应用 binding 只暴露 `Get()`；未暴露 SQL、文件系统变更、任意网络、Secret resolve 或数据库实例。
- `internal/desktop/events.go` 实现版本 `1` 的 `core:event` envelope：加密随机 ID、UTC RFC3339Nano 时间；熵失败 fail-closed 并跳过事件。
- 启动失败在 DB/FileStore 初始化前附着可诊断安全 health；正常路径以 DB-backed health 替换；无 `context.Background()` 伪造调用链。
- Wails 生成产物已由 `wails generate module` 产生且未手改；前端 binding 表面审计通过。
- 独立 spec/quality 审核问题均已修复并复审通过。

### WP-01 Task 8 — APPROVED

- React Query key 固定为 `['desktop-health']`，health 事实未放入 Zustand。
- browser/Vite 模式不静态导入 generated Wails modules、不查询、不订阅事件、不显示 health badge、无额外网络请求。
- binding 可用性与 event runtime 可用性拆分；仅 `{ version: 1, type: 'health.changed' }` 失效 query。
- 动态 import 失败后会清除缓存，允许重试。
- `DesktopHealthStatus` 提供英文/中文、加载/安全模式/不可用/就绪状态、文本+图标、焦点可达诊断 tooltip；错误不会显示“Local database ready”。
- 独立 spec/quality 审核问题均已修复并复审通过。

### WP-01 Task 9 — IMPLEMENTED, FINAL ACCEPTANCE BLOCKED

已经取得的真实证据：

- `D:\GoWorks1.18\bin\wails.exe build -clean` PASS；产物为 Windows/amd64 生产二进制，无外部 Vite server 依赖。
- 已记录产物：`build/bin/InfiniteAtelier.exe`，大小 `26,524,672` bytes，SHA-256 `a52e7add9ea7959ee948466998400cffdc5ecdfe53b8d9c94c5c3e897519d1d5`（该历史 fingerprint 仅在同一产物仍存在且未重建时适用）。
- 隔离原生启动显示 `/` 首页和 ready health 指示器。
- 单实例：第二个启动进程退出，原进程仍存在。
- graceful close/restart：两个 owned process 均以主窗口关闭退出，未强制终止。
- 隔离 SQLite 在重启后仅包含 migration version `1`。
- JSONL 启动日志经过非打印敏感指标扫描，未发现 Authorization/Bearer/API-key/SQL 指标，且只记录安全的 `component=desktop`、`database=ready`。
- smoke 前确认不存在需处理的未归属 `InfiniteAtelier.exe`；用户先前授权的关闭边界未被超出。之后仅关闭已验证为本次 owned、路径精确匹配的进程。

**仍然缺失的 Task 9 计划验收：**

| 路由 | 可信直接原生渲染证据 | 当前结果 |
|---|---|---|
| `/` | 有：隔离 production Wails 窗口截图与 ready 状态 | PASS |
| `/assets` | Windows WebView 未暴露可用 DOM/ARIA 控件；一次基于可见标签的 owned-window 坐标点击未发生转场 | BLOCKED |
| `/canvas` | 无可信原生路由转场/渲染观测 | BLOCKED |
| `/director` | 无可信原生路由转场/渲染观测 | BLOCKED |
| `/config` | 无可信原生路由转场/渲染观测 | BLOCKED |

边界说明：Windows UI Automation 只公开 Wails/WebView container（如 `BrowserRootView`、`EmbeddedBrowserTabRootView`），没有 HTML/ARIA 导航控件；WebView 的 `SetFocus()` 失败。不能使用源码中的 route registration、无证据的 raw coordinates、测试专用路由或未授权操作未归属进程来替代上述原生证据。需要在具备可信 WebView/browser 自动化能力的隔离环境，或由可信人工原生观察，完成四个路由的直接 render 记录。

### 当前测试/环境结论

| 命令 / 检查 | 结果 |
|---|---|
| `go test ./... -count=1` | PASS（Task 9 evidence） |
| `go vet ./...` | PASS（Task 9 evidence） |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\verify.ps1` | PASS（当前范围；frontend test/lint 为显式 SKIP） |
| `go test ./internal/infrastructure/database -run 'Test.*(Migration|Safe|Failure)' -count=1 -v` | PASS |
| `go test . -run 'TestStartup.*SafeHealth' -count=1 -v` | PASS |
| `go test -race ./... -count=1` | **ENVIRONMENT FAIL**：`C:\MinGW\bin\gcc.exe` 为 32-bit GCC，无法编译 amd64 CGO：`cc1.exe: sorry, unimplemented: 64-bit mode not compiled in`。不能报告为通过，也不能为规避此问题添加第二 SQLite driver。 |

## 5. WP-01 的剩余工作与安全恢复顺序

### 先决阻塞：Task 9 收尾（不得跳过）

1. 保留现有 production binary/source 现场并核对实际 hash；若重建，重新记录 hash 与 build evidence。
2. 在新的 owned temp profile/app-data 根目录启动 exact production binary。
3. 使用可信的原生 WebView/browser 控制能力或可审计人工观察，逐个验证 `/assets`、`/canvas`、`/director`、`/config` 已实际渲染。
4. 每一步记录 route、隔离条件、观测方法、结果；不接触用户真实 profile、数据库、密钥或媒体。
5. 由独立只读 spec reviewer 审核 Task 9 计划和 AC-FOUND-001 逐项满足情况；有发现先修复/补证并复审。
6. 再由独立只读 quality reviewer 审核；只有两个 reviewer 都 APPROVED，Task 9 才能结束。

### Task 10 — verify、CI 与 README（未开始；Task 9 批准后才可开始）

- 更新 `scripts/verify.ps1`、`scripts/verify.sh`、`.github/workflows/desktop-build.yml` 与 `README.md`。
- CI 使用 Go 1.25；Windows Wails job 锁定 Wails v2.15.0。
- PowerShell/POSIX 脚本应保持真实 PASS/FAIL/SKIP；POSIX 不得在 `web` 目录错误执行 Go 命令。
- 未获得证据时，不宣称 macOS/Linux desktop package 通过。
- 按同一 implementer → 独立 spec → 独立 quality 串行门禁执行。

### Task 11 — WP-01 证据关闭（未开始；Task 10 批准后才可开始）

- 运行最终 `git diff --check`、`git status --short --branch`、可用全量 Go/verify、Wails clean build 与原生 smoke。
- 如实记录 `go test -race` 环境失败，不能标 PASS。
- 严格评估 AC-FOUND-001、AC-FOUND-002、AC-FOUND-003。
- 更新 `docs/implementation/STATUS.md`、`docs/implementation/TRACEABILITY.md`、`docs/adr/0001-desktop-framework.md`（仅证据改变时）与 `docs/adr/0002-sqlite-driver-and-migrations.md`。
- traceability 需按当前 PRD FR 标题重新校准。
- 检查 secret、临时文件、大文件、生成物和许可证事项。
- 只有验收均真实通过，才能把 WP-01 标记 COMPLETE；结束后停止，仅推荐 WP-02，等待用户明确授权。

## 6. 对标 PRD 的 WP-02–WP-12 完整后续任务

下列工作包都尚未开始，也没有获得当前会话的实施授权。依赖顺序见 `docs/ROADMAP.md`，不得跨包偷跑。

| 工作包 | PRD 对齐目标 | 主要待开发能力 | 关键验收方向 |
|---|---|---|---|
| WP-02 | FR-140 Provider Gateway、FR-170 Backup、FR-180 diagnostics；NFR-003 | SecretStore Port/native/fake，前端 masked/ref-only，受控 HTTP/SSRF/TLS/redirect/body limits，Provider registry/error taxonomy，OpenAI-compatible text stream，Mock provider，迁移旧前端 key/script 路径并加 feature flag，动态代码/secret CI scan | AC-FOUND-004、AC-FOUND-005 文本与安全子集、AC-SEC-001 |
| WP-03 | FR-050/FR-140/FR-150/FR-160；NFR-002 | durable GenerationJob/attempt/state machine、scheduler/worker/lease/retry/recovery、image/Gemini/video/audio contracts、结果安全进入 FileStore、Job Center、批量暂停/取消/仅失败重试、禁止新增 browser-direct provider | AC-FOUND-006、AC-MEDIA-001 mock、重启恢复、幂等、失败不假成功 |
| WP-04 | FR-001、FR-020、FR-130、FR-160、FR-170；NFR-004 | legacy manifest/fixture、Project/Canvas/Asset backend、ID mapping、snapshot、node/edge/viewport/chat/history/media import、unsupported metadata preservation、idempotent import、Repository canvas adapter、Zustand 只保留 UI、普通无密钥 backup v1、canvas E2E | AC-LEGACY-001/002/003、AC-CANVAS-004、AC-BACKUP-001 基础 |
| WP-05 | FR-020、FR-030、FR-040、FR-050、FR-060、FR-070、FR-100、FR-110、FR-130 | Drama Project/Settings、Source/Chapter、Story entity/event/relation/conflict、Episode/Skeleton/Strategy/Script/Scene/Dialogue/Shot、Asset/version/lineage、Director/Storyboard、Workflow/Review/Gate aggregates、Studio shell/creator、domain commands/queries/events/migrations | domain constraints、唯一 approved、locked/stale、drama creation、AC-CANVAS-001/002 子集 |
| WP-06 | FR-020、FR-030；NFR-002/NFR-003 | TXT/Markdown/DOCX import、encoding/normalization、chapter detection/edit/confirm、source offsets、大文本分块、candidate event/entity workflow、accept/reject/edit/lock、aliases/conflicts、story graph、prompt injection boundary、恶意 DOCX/大文件测试 | AC-STORY-001/002、100k 中文字符 UI 不阻塞、无文档指令执行 |
| WP-07 | FR-090、FR-100、FR-110、FR-120 | Decision/Execution/Supervision runtime、Skill registry/version/manifest、Tool ACL/schema、AgentRun/ToolCall、JSON validation/one repair、Workflow/Stage/Review/Gate state machine、limits/cancellation、deterministic Mock LLM、Agent/Quality UI、recent memory port | AC-AGENT-001..005、workflow DB state、supervisor read-only、CI 无真实模型 |
| WP-08 | FR-040、FR-100、FR-110 | ScriptAgent decision、story skeleton/adaptation/script skills and schemas、supervisor rules、PASS/FIX/REDO/MANUAL_EDIT、immutable revisions/diff/locked fields、Scene/Dialogue/Shot persistence、Script UI/canvas projection、model policy、canary drama E2E | AC-SCRIPT-001/002/003、可恢复/可追溯、locked 不被 FIX 覆盖 |
| WP-09 | FR-050、FR-060、FR-070、FR-130 | ProductionAgent decision、director plan、asset gap/create/import/candidate/approve/lineage、storyboard table/panel/supervision、image job batching、shot/asset/panel projection、MONOFORM typed bridge、impact analysis、production E2E | AC-ASSET-001/002、AC-BOARD-001/002/003、缺 asset 阻止批量、单 shot redo 隔离 |
| WP-10 | FR-110、FR-120 | persistent scoped memory、summary provenance、FLOAT32 vector/index/threshold/topK/fusion/self-hit exclusion/deep recall、inspector/delete、episodic/semantic/procedural/artifact separation、deterministic consistency checks、Quality Center、embedding ADR | AC-MEM-001..005、AC-E2E-004/005、跨项目泄露为 0 |
| WP-11 | FR-080 | video contract/UI、first/last/reference assets、shot video versions、TTS/dialogue mapping、audio assets、SRT/VTT、timeline、FFmpeg ADR and safe parameterization、MP4 and manifest exports、final supervisor/recovery/E2E | AC-MEDIA-001/002/003、可播放 output、无 shell injection、可追溯 export |
| WP-12 | FR-001、FR-170、FR-180、NFR-001..006 | complete verification, security/integration/E2E/performance suites, 1k-node/2k-edge and asset/memory benchmarks, backup/restore atomicity, migration from earlier package, Windows clean VM/installer/signing, SBOM/notices, secret/vulnerability scans, safe mode/recovery, docs/release checklist, remove/unreach dangerous legacy paths | PRD release blockers 全清、Windows install/upgrade/recovery、all acceptance scenarios |

## 7. PRD 发布阻断条件

在下列任一项未清零前不得发布：

1. 前端或普通备份可读取完整 API Key。
2. 任意模型 JavaScript 执行路径仍可达（包括 `new Function`/`eval` 等）。
3. Provider/API proxy 可访问 loopback、private network 或任意未授权地址。
4. Workflow 状态仅存于内存。
5. 视频结果未验证文件即标记成功。
6. DB migration 无 snapshot 和可靠 rollback/forward recovery。
7. Supervisor 可调用未授权写工具。
8. 导入存在 path traversal、Zip Slip 或 Zip Bomb。
9. Legacy migration 静默丢失。
10. 核心 E2E 未通过。
11. MIT/third-party notices/SBOM 缺失或不完整。

## 8. 可直接复制的新会话恢复提示词

```text
你正在以下仓库继续开发：
F:\AI_Movie_Things_202606\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\infinite-atelier-mine-0903

使用已安装的 Ponytail 完整工作流，并采用严格串行门禁：每一个 Task 必须按 implementer → 独立只读 spec reviewer → 独立只读 quality reviewer 完成。reviewer 发现必须修复，且重新审查通过后才能进入下一个 Task。不要并行实现多个 Task；若无法获得真正独立的 reviewer，停止并如实报告能力缺口，不得由 implementer 代替 reviewer。

当前仅获授权执行 WP-01。先完整阅读：PRD.md、AGENTS.md、docs/implementation/STATUS.md、docs/ROADMAP.md 当前 WP、docs/ARCHITECTURE.md、docs/DOMAIN_MODEL.md、docs/AGENT_CONTRACTS.md、docs/SECURITY.md、docs/ACCEPTANCE.md、docs/reference/INTEGRATION_ANALYSIS.md、docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md、README/package/config/CI/相关代码和测试，以及 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md、docs/implementation/wp01-execution-2026-09-07.md 和最新 docs/implementation/handoff-2026-09-08-151249+0800.md。

开始前必须执行并记录：
- git status --short --branch
- git diff --cached --name-status
- git diff --name-status
- git diff --check
- 当前可运行的 WP-01 基线命令

保护所有现有 tracked 与 untracked 文件。禁止 commit、push、stash、reset、clean；不得覆盖用户文件；不得访问/修改真实数据库、密钥、浏览器资料、WebView profile 或媒体；不得调用真实 Provider。Toonflow 仅能 clean-room 参考。不要加入 ORM、第二 SQLite driver、Secret/Provider/Job/Agent/Drama 等超出当前 Task 的生产代码。构建后恢复零字节 web/dist/.gitkeep。

事实状态：
- 分支 codex/wp-01-desktop-foundation；HEAD a243891455ec17687dd54b5ac90d3bd64478a1a1。
- WP-00 COMPLETE。
- WP-01 Task 1–8 已获独立 spec/quality 批准。
- WP-01 Task 9 已实现并取得 production build、首页原生 render、single instance、close/restart、schema persistence 和 log hygiene 证据；但 `/assets`、`/canvas`、`/director`、`/config` 没有可信的直接原生 Wails render 证据。
- Task 9 现在是唯一阻塞点；不得开始 Task 10 或 Task 11。
- Windows UI Automation 仅暴露 WebView container，未暴露可用 DOM/ARIA controls，SetFocus 失败。不可用源码路由注册、盲点坐标点击或测试专用路径代替 native evidence。
- go test -race ./... 在本机因 C:\MinGW\bin\gcc.exe 的 32-bit GCC 无法编译 amd64 CGO 而失败。该结果只能写为 environment limitation/FAIL，不能称 PASS，也不能据此加入第二 SQLite driver。

请只恢复 Task 9：
1. 在新的、owned 的临时 app-data/WebView profile 环境内启动精确的 production binary，绝不操作未归属用户进程。
2. 使用可信 WebView/browser automation 或可审计的人工原生观察，逐个验证 `/assets`、`/canvas`、`/director`、`/config` 真正渲染。
3. 每项记录隔离环境、方法、route、原生观测和结果；任何路由未观察到都保留 BLOCKED。
4. 完成后由独立 spec reviewer 审核 Task 9；若有问题修复/补证并复审。spec APPROVED 后再由独立 quality reviewer 审核。
5. 两者都 APPROVED 前不得进入 Task 10。若 Task 9 无法在现有环境安全证明，更新 STATUS/执行证据，明确 BLOCKED 并停止，不要绕过。

只有 Task 9 被独立双审批准后，才可按 Task 10、Task 11 串行继续。WP-01 完成后，按 AGENTS.md 固定报告格式停止，仅推荐 WP-02，等待用户明确授权；不得自动启动 WP-02。
```

## 9. Comet 状态

- `.comet/current-change.json` 当前仍指向旧 change：`wp-01-session-handoff-20260908`。
- 该旧 change 的 brief/spec/verification 仍基于“Task 7 中途”的历史事实，不能用于描述当前恢复点，也不能修改为当前 Task 9 的结论。
- 本 handoff 对应的新的 Comet change 应使用 `wp-01-current-state-handoff-20260908`，作为文档工件变更；它不表示产品实现、Task 9 或 WP-01 已完成。

## 10. 结束边界

本交接文件只记录当前真实状态。WP-01 仍是 PARTIAL/BLOCKED，WP-02–WP-12 均未开始。下一位执行者应先补齐或如实确认 Task 9 原生路由证据阻塞，不能跳到 CI、最终验收或任何后续工作包。
