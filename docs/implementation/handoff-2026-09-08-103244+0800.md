# Infinite Atelier Drama Studio 开发交接

> 交接时间（Asia/Shanghai）：2026-09-08T10:32:44+08:00  
> 仓库：`F:\\AI_Movie_Things_202606\\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\\infinite-atelier-mine-0903`  
> 分支：`codex/wp-01-desktop-foundation`  
> HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`  
> 当前工作包：WP-01 — Wails v2、Go Core、SQLite 与文件底座  
> 精确恢复点：Task 7 implementer 中途；Health/event 代码与定向 Go 测试已通过，`wails generate module` 被中断，独立 spec/quality 双审未开始

## 1. 一句话交接

WP-00 已完成。WP-01 Task 1–6 均已实现并通过独立规格与质量审查。Task 7 已写入 Health Snapshot/Probe、Wails-only `Get` binding、`core:event` envelope，并在 `app.go`/`main.go` 接线；`go test ./internal/application/health ./internal/desktop ./` 通过。用户随后中断/拒绝 `wails generate module`，`web/src/wailsjs` 不存在，Task 7 双审未做。Task 8–11 与 WP-02–WP-12 均未开始。新会话必须从 Task 7 implementer 继续：先生成并审计 bindings，再独立双审；Task 7 批准前不得进入 Task 8；完成 WP-01 后停止，不得自动进入 WP-02。

## 2. Git 与数据安全现场

- 分支与 HEAD 保持上述值；没有 WP-01 commit。
- staged diff 为空。
- tracked 修改仍为 `.gitignore`、`web/package.json`、`web/package-lock.json`。
- 大量 WP-00/WP-01、规范、Comet 和根目录文件为 untracked；untracked 不表示可删除。
- `git diff --check` 退出 0；仅有既存 LF/CRLF 警告和无法访问宿主全局 ignore 的警告。
- 未执行 commit、push、stash、reset、clean；未改动真实数据库、浏览器资料、密钥、媒体或 Provider。
- `web/dist/.gitkeep` 仍存在。

交接时关键状态：

```text
## codex/wp-01-desktop-foundation
 M .gitignore
 M web/package-lock.json
 M web/package.json
?? .agents/
?? .codex/
?? .comet/
?? AGENTS.md
?? PRD.md
?? THIRD_PARTY_NOTICES.md
?? app.go
?? app_test.go
?? docs/
?? go.mod
?? go.sum
?? internal/
?? main.go
?? scripts/verify.ps1
?? scripts/verify.sh
?? wails.json
?? web/dist/
```

这只是压缩摘要；完整 tracked/untracked 清单以 `git status --short --branch` 为准。

## 3. 必读事实与文档漂移

新会话修改前必须完整按 AGENTS.md 顺序阅读：

1. `PRD.md`
2. `AGENTS.md`
3. `docs/implementation/STATUS.md`
4. `docs/ROADMAP.md` 当前 WP-01
5. `docs/ARCHITECTURE.md`
6. `docs/DOMAIN_MODEL.md`
7. `docs/AGENT_CONTRACTS.md`
8. `docs/SECURITY.md`
9. `docs/ACCEPTANCE.md`
10. `docs/reference/INTEGRATION_ANALYSIS.md`
11. `docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md`
12. README、package、配置、CI、相关代码和测试
13. `docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md`
14. `docs/implementation/handoff-2026-09-05-163956+0800.md`
15. `docs/implementation/handoff-2026-09-07-161115+0800.md`
16. `docs/implementation/wp01-execution-2026-09-07.md`
17. 本 handoff

必须保留以下纠正：

- `STATUS.md` 中 “WP-01 implementation not started” 已过时。
- Wails v2.15.0 和 modernc SQLite v1.58.0 都要求 Go 1.25；计划中 Go 1.24 示例已过时。
- `wailsjsdir=web/src` 是正确配置，Wails 会在其下追加 `wailsjs`。
- TRACEABILITY 必须按当前 PRD 标题重校：FR-010 桌面、FR-020 项目/原著/章节、FR-160 素材存储、FR-180 设置/日志/诊断/隐私。
- ADR-0002 仍为 Proposed；Task 4 只完成驱动矩阵，迁移/快照/生产打包证据属于后续 Task，不得提前 Accepted。
- Toonflow 仅 clean-room 思想参考。

## 4. 已完成工作

### WP-00 — COMPLETE

仓库审计、基线、追踪矩阵、桌面与 SQLite ADR 初稿、Windows/POSIX verify 脚本已完成。

### WP-01 Task 1 — APPROVED

- npm lock 修复完成，独立 spec/quality 通过。
- 现有 `web/package.json` 与 `web/package-lock.json` 属于需保留的用户现场。

### WP-01 Task 2 — APPROVED

- Wails v2.15.0 最小桌面壳、Go 1.25 module、`wailsjsdir=web/src`。
- 独立 spec/quality 通过。

### WP-01 Task 3 — APPROVED

- AppError、appdirs、结构化脱敏日志、生命周期；普通引号内敏感赋值 TDD 修复。
- 独立 spec/quality 最终 APPROVED。

### WP-01 Task 4 — APPROVED

- `modernc.org/sqlite` v1.58.0 是唯一直接 SQLite 驱动；`go mod tidy` 完成后与 Wails v2.15.0 同为 direct require。无 ORM/第二驱动。
- Driver URI DSN、WAL/FK/busy_timeout=5000、pool=1、安全 `DATABASE_OPEN_FAILED`、取消打开不建文件。
- `go test ./internal/infrastructure/database -run TestDriver -count=20` PASS，SQLite 3.53.4。
- `CGO_ENABLED=0` 定向测试 PASS，调用者 CGO 恢复为 unset。
- `go test -c` windows/linux/darwin amd64 PASS，二进制未提交并已删除。
- `go test ./...`、`go vet`、`go build`、`gofmt`、`git diff --check` PASS。
- `go test -race` FAIL：MinGW.org GCC 6.3.0 32 位，`cc1.exe: sorry, unimplemented: 64-bit mode not compiled in`。环境限制，未引入 mattn。
- ADR-0002 保持 Proposed。独立 spec（Carver）与 quality（Kuhn）均 APPROVED。

### WP-01 Task 5 — APPROVED

- 仅 `schema_migrations` / `file_objects` / `file_references`；embed runner、SHA-256 checksum、单事务、数字版本排序。
- 非空升级 `VACUUM INTO` + `SELECT quote(?)` + fsync；路径含撇号。
- 失败关闭可写句柄，返回 `ModeSafe` 与稳定诊断；不删除/自动恢复。
- OnStartup（单实例锁之后）打开 DB；Close 先 `wal_checkpoint(TRUNCATE)` 再 close，并 `errors.Join` checkpoint 错误。
- integrity 夹具：插入 `file_objects` 后翻转 payload 页字节，断言仅 `DATABASE_INTEGRITY_FAILED`。
- 独立 spec（Meitner，复审 Lovelace）与 quality（Dewey 要求修改后 Pauli）均 APPROVED。

### WP-01 Task 6 — APPROVED

- `application/files` Store/Repository/Import；filestore 内容寻址、512 字节 MIME、SHA-256、分片目录、原子 rename、去重。
- 显示名只校验；拒绝 traversal/绝对路径/分隔符/colon/device/NUL/空名。
- 取消/失败 reader 在 Windows 上先 Close 再 Remove owned temp。
- Open 仅 `[0-9a-f]{64}`；缺失 `FILE_NOT_FOUND`，不泄漏路径。
- SQLite 参数化 upsert，hash/storage/size 冲突 fail closed；缺失 object 的 reference FK 失败。
- 独立 spec（Aristotle）与 quality（Huygens）均 APPROVED。

## 5. 当前中断点：WP-01 Task 7

### 已落盘但尚未获批准

- `internal/application/health/service.go` / `service_test.go`
  - Snapshot JSON DTO：Version、Database、DataDirectory、SafeMode、Diagnostic。
  - 小 Probe 接口；ready/safe_mode；不泄漏 DB 路径或 SQL cause。
  - 传入 `*sql.DB` 前有 typed-nil 防护。
- `internal/desktop/health_binding.go` / `health_binding_test.go`
  - 只导出 `Get()`；`Attach` 是包级函数，不是 Wails method。
- `internal/desktop/events.go` / `events_test.go`
  - Envelope v1：随机 ID、UTC RFC3339Nano、事件名仅 `core:event`。
- `app.go`：startup 在 DB/FileStore 后 Attach health；domReady 发 `health.changed`。
- `main.go`：`Bind: []interface{}{application.health}`，`emit = wailsruntime.EventsEmit`。

### 已验证

- `gofmt -w` 后 `go test ./internal/application/health ./internal/desktop ./ -count=1 -timeout 60s` PASS。

### 仍必须完成

1. 运行 `wails generate module`，生成 `web/src/wailsjs/`（当前 `Test-Path web\\src\\wailsjs` = False）。
2. 审计生成文件：只应有预期 health method/model 与 Wails runtime；搜索 `apiKey`/`secret`/`Resolve`/SQL/文件系统变更/任意网络方法，期望无。不可手改生成文件。
3. 若构建删除 `web/dist/.gitkeep`，验证后必须恢复该空占位。
4. `go test ./...`、`go vet`、`gofmt`、`git diff --check`、Secret/生成物检查。
5. 独立只读 spec reviewer，修复并复审后再独立只读 quality reviewer。
6. 两者 APPROVED 后才能进入 Task 8。

`wails generate module` 在本会话被用户中断/拒绝（`Rejected("rejected by user")`），不要把它记为 PASS。

## 6. WP-01 剩余任务清单

### Task 7 — Health、binding、core:event（IN PROGRESS）

- 映射：FR-010、FR-180、AC-FOUND-001。
- 完成 Wails generate/审计与独立双审。

### Task 8 — React Query health 与 browser fallback

- 映射：FR-010、FR-180。
- runtime-safe adapter；无 Wails globals 时 null/no-op。
- TanStack Query，订阅并清理 `core:event`，健康事实不进 Zustand。
- UserStatusActions 文本+图标 ready/loading/safe mode；键盘、焦点、screen reader；状态不得只靠颜色。
- en-US/zh-CN i18n；safe mode tooltip 含 diagnostic。
- Vite/browser 模式无 Wails global 异常、无错误 badge。

### Task 9 — Windows production build 与 smoke

- 映射：FR-010、AC-FOUND-001。
- `wails build -clean`；隔离临时 APPDATA/LOCALAPPDATA/WebView profile。
- 原生 smoke：单窗口、health ready、`/` `/assets` `/canvas` `/director` `/config`；第二实例聚焦第一实例；关闭退出；重启 DB version 不变；日志无敏感内容。
- safe mode 只通过测试临时目录注入坏 migration。
- 记录 exe 路径/大小/SHA-256；构建后恢复 `web/dist/.gitkeep`。

### Task 10 — verify、CI 与 README

- 映射：FR-010、NFR 验证。
- PowerShell/POSIX verify；POSIX 不得停在 web 目录后错误执行 Go。
- Go/Wails 不可用时明确 SKIP；失败必须非零。
- CI Go 版本必须为 1.25；Windows job 安装 Wails v2.15.0。
- 未获证据前不宣称 macOS/Linux 桌面 packaging 已通过。

### Task 11 — 证据关闭 WP-01

- 严格评估 AC-FOUND-001/002/003。
- 更新 STATUS、TRACEABILITY、ADR-0002；保留 WP-00 历史并校正 FR 标题与 Go 版本。
- 按 AGENTS.md 固定格式报告；推荐 WP-02，停止等待明确批准。

## 7. 对标 PRD 的 WP-02–WP-12 完整后续清单

以下均未获本会话实施授权。

### WP-02 — Secret、网络安全与 Provider Gateway 基础

- 映射：FR-140、FR-170、FR-180、NFR-003；AC-FOUND-004/005、AC-SEC-001。
- SecretStore fake/native；前端只读 Secret 状态。
- 受控 HTTP client 与 SSRF corpus；OpenAI-compatible Text Generate/Stream。

### WP-03 — Persistent Job Manager 与多模态 Provider

- 映射：FR-140、FR-150、FR-160、FR-080 子集、NFR-002；AC-FOUND-006、AC-MEDIA-001 子集。

### WP-04 — Legacy 项目迁移与画布后端适配

- 映射：FR-001、FR-130、FR-160、FR-170、NFR-004；AC-LEGACY-001/002/003、AC-CANVAS-004、AC-BACKUP-001 基础。

### WP-05 — 短剧领域模型与 Studio UI Shell

- 映射：FR-020/030/040/050/060/070/100/110/130。

### WP-06 — 原始文档、章节与事件图谱

- 映射：FR-020、FR-030、NFR-002/003；AC-STORY-001/002。

### WP-07 — Agent Runtime、Skill、Workflow 与 Quality Gate

- 映射：FR-090/100/110/120；AC-AGENT-001..005。

### WP-08 — ScriptAgent：骨架、策略与剧本

- 映射：FR-040、FR-100、FR-110；AC-SCRIPT-001/002/003。

### WP-09 — ProductionAgent：资产、导演与分镜

- 映射：FR-050/060/070/130；AC-ASSET-001/002、AC-BOARD-001/002/003。

### WP-10 — Persistent Memory、Consistency 与 Quality Center

- 映射：FR-110、FR-120；AC-MEM-001..005、AC-E2E-004/005。

### WP-11 — 视频、音频、字幕、时间线与导出

- 映射：FR-080；AC-MEDIA-001/002/003。

### WP-12 — 硬化、性能、打包与 Release Candidate

- 映射：FR-001、FR-170、FR-180、NFR-001..006 与全部发布阻断条件。

## 8. PRD 发布阻断条件

以下任一存在都不得发布：

1. 前端或普通备份能获得完整 API Key。
2. 仍有任意模型 JavaScript 执行路径。
3. API 代理可访问回环、私网或任意地址。
4. Workflow 状态只存在内存。
5. 视频结果未验证即标成功。
6. DB migration 无备份或回滚/修复路径。
7. Supervisor 可调用未授权写工具。
8. 导入可 path traversal 或 Zip Bomb。
9. 旧项目迁移静默丢失。
10. 核心 E2E 未通过。
11. License/third-party notices 缺失。

## 9. 新 Codex 或 Cursor 会话完整提示词

复制以下整段作为新会话首条消息：

````text
你正在以下现有仓库继续开发：
F:\\AI_Movie_Things_202606\\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\\infinite-atelier-mine-0903

请使用已安装的 Ponytail 完整工作流。明确授权你使用子代理，但必须按单个任务串行执行：implementer → 独立 spec reviewer → 独立 quality reviewer。不要并行实现多个任务；当前任务的任何审查问题必须先修复并复审。若当前平台不支持真正独立的子代理/会话审查，必须停止并说明能力缺口，不得由 implementer 冒充独立 reviewer。

当前目标：继续执行已获批准的 WP-01，仅从 Task 7 的中途现场恢复。Task 7 获得规格和质量最终批准后，再按现有计划串行执行 Task 8–11。完成 WP-01 后停止，按 AGENTS.md 固定格式报告，推荐 WP-02 但等待明确批准；不要自动进入 WP-02。

开始任何修改前必须：

1. 完整按顺序阅读 PRD.md、AGENTS.md、docs/implementation/STATUS.md、docs/ROADMAP.md 当前 WP-01、docs/ARCHITECTURE.md、docs/DOMAIN_MODEL.md、docs/AGENT_CONTRACTS.md、docs/SECURITY.md、docs/ACCEPTANCE.md、docs/reference/INTEGRATION_ANALYSIS.md、docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md，以及 README/package/config/CI/相关代码和测试。
2. 完整阅读 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md、docs/implementation/handoff-2026-09-07-161115+0800.md、docs/implementation/wp01-execution-2026-09-07.md，以及最新的 docs/implementation/handoff-2026-09-08-103244+0800.md。
3. 运行 git status --short --branch、git diff --cached --name-status、git diff --name-status、git diff --check，记录并保护全部现有 tracked/untracked 修改。
4. 不要相信 STATUS.md 中“WP-01 implementation not started”的过时文字；以最新 handoff、实际 Git、测试和审查结论为准。不得删除、覆盖、stash、reset 或 clean 现有现场。

当前确认事实：

- 分支 codex/wp-01-desktop-foundation；HEAD a243891455ec17687dd54b5ac90d3bd64478a1a1；没有 WP-01 commit。
- Task 1–6 已完成并通过独立规格/质量审查。
- Task 4：modernc.org/sqlite v1.58.0 为唯一直接 SQLite 驱动；CGO_ENABLED=0 PASS；windows/linux/darwin go test -c PASS；ADR-0002 仍为 Proposed。
- Task 5：foundation migration、VACUUM INTO snapshot、ModeSafe、integrity fail-closed、checkpoint+close 已批准。
- Task 6：content-addressed FileStore、metadata upsert、FK 与安全文件名已批准。
- Task 7 仍为 IN PROGRESS：Health service/binding/event 与 app/main 接线已落盘；go test ./internal/application/health ./internal/desktop ./ PASS。wails generate module 被中断，web/src/wailsjs 不存在；独立双审未开始。不得把 Task 7 标为完成。
- go test -race 因 C:\\MinGW\\bin\\gcc.exe 为 32 位、无法编译 amd64 CGO 而失败；必须记录为环境限制，不能伪装通过，也不能仅因此添加 mattn 第二驱动。

先完成 Task 7 implementer：

1. 核对现有 health/desktop/app/main 代码，保留正确内容，不从头重写。
2. 运行 wails generate module，生成 web/src/wailsjs；不可手改生成文件。
3. 审计 bindings：仅预期 health method/model 与 runtime；无 apiKey/secret/Resolve/SQL/文件系统变更/任意网络方法。
4. 跑 go test ./...、go vet ./...、go build ./...、gofmt、git diff --check；race 如实记录环境失败。
5. 若构建删除空的 web/dist/.gitkeep，必须恢复。
6. implementer 完成后，依次启动独立只读 spec reviewer、独立只读 quality reviewer；任何发现先修复并重新审查，两者 APPROVED 后才进入 Task 8。

Task 8–11 严格按 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md：

- Task 8：React Query health adapter/status、browser mode 安全回退、i18n/a11y。
- Task 9：隔离用户目录的 Windows production build/native smoke。
- Task 10：verify/CI/README；CI Go 版本必须为 1.25，保留真实 SKIP/FAIL 语义。
- Task 11：最终 AC-FOUND-001/002/003、STATUS/TRACEABILITY/ADR 更新与安全检查；TRACEABILITY 按当前 PRD 标题重校。

Git/数据安全：

- 不执行 commit/push/stash/reset/clean，除非用户另行明确要求。
- 不覆盖用户文件，不修改真实数据库、密钥、浏览器数据或媒体，不访问真实 Provider。
- 一次只做一个 Task；不得用未来 Task 的代码补当前验收。
- 不引入 ORM、第二 SQLite 驱动、Provider/Secret/Job/Agent/Drama 等超出当前 Task/WP-01 的代码。
- Toonflow 仅 clean-room 参考。
- 构建可能删除空的 web/dist/.gitkeep；验证后必须恢复这个已知空占位文件。

请先用一条简短 commentary 说明你已完整读取最新 handoff 并准备从 Task 7 恢复，然后直接开始。持续工作到 WP-01 完成或出现真正需要用户决策的阻塞；完成后停止，不要进入 WP-02。
````

## 10. 新会话第一组命令

```powershell
git status --short --branch
git diff --cached --name-status
git diff --name-status
git diff --check
go env GOVERSION GOTOOLCHAIN CGO_ENABLED GOOS GOARCH CC GOMODCACHE GOPATH
go test ./internal/application/health ./internal/desktop ./ -count=1
wails generate module
go test ./... -count=1
go vet ./...
go build ./...
```

`wails generate module` 若因沙箱或权限失败，按宿主审批机制重跑；不要手改生成的 wailsjs。

## 11. Comet Native 记录

- Workflow：Native。
- Change：`wp-01-session-handoff-20260908`。
- Workspace：current，绑定现有 `codex/wp-01-desktop-foundation`。
- 本 change 只生成与验证交接工件，不继续产品实现。
- 正式 Shape artifacts：`docs/comet/changes/wp-01-session-handoff-20260908/`。
- Archive 完成状态以 `comet-state.yaml` 与 `verification.md` 为准；本文生成时 Comet 尚处于 Shape/Build，不能提前声称已归档。

## 12. 完成边界

本 handoff 的完成只表示交接材料准确可复用。Task 7、Task 8–11 和 WP-01 仍未完成。新会话必须以测试和独立审查证据推进，不能因已有草稿、能编译或文档存在而标记 COMPLETE。

