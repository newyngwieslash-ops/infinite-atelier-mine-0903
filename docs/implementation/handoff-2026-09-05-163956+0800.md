# Infinite Atelier Drama Studio 开发交接

> 交接时间（Asia/Shanghai）：2026-09-05T16:39:56+08:00
> 交接时间（UTC）：2026-09-05T08:39:56Z
> 仓库：`infinite-atelier-mine-0903`
> 当前分支：`codex/wp-01-desktop-foundation`
> HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`
> 当前工作包：WP-01 — Wails v2、Go Core、SQLite 与文件底座
> 当前恢复点：WP-01 Task 3 最终质量修复

## 1. 一句话交接

WP-00 已完成；WP-01 Task 1 和 Task 2 已实现并通过独立规格/质量审查；Task 3 的主体、测试和规格审查已通过，但最终质量复审仍有一个 Important 日志脱敏漏洞，因此 Task 3 尚未获准完成。新会话应先修复这一项、重新通过规格与质量复审，再按既有计划串行执行 Task 4–11；完成 WP-01 后必须停止，不得自动进入 WP-02。

## 2. Git 与数据安全现场

- 当前分支：`codex/wp-01-desktop-foundation`。
- 当前 HEAD 与原始基线相同：`a243891455ec17687dd54b5ac90d3bd64478a1a1`（`fix: prevent repeated Node.js setup prompts`）。
- 本轮及此前 WP-01 实施均未 commit、push、stash、reset 或 clean。
- staged changes：无已知 staged 变更；新会话必须重新运行 `git diff --cached --name-status` 证实。
- tracked 修改：`.gitignore`、`web/package.json`、`web/package-lock.json`。
- Wails/Go、WP-00 文档、规格包、脚本、Comet 工件等当前大多为 untracked；不得把 untracked 等同于可删除文件。
- 所有现有未跟踪规格、WP-00 交付物、WP-01 实现和用户文件都必须保留。
- 未访问或修改真实用户数据库、浏览器数据、媒体、Provider 配置或密钥。
- 未调用真实付费 Provider。
- 不得执行 `git reset --hard`、`git clean -fd`、强制 checkout 覆盖、自动 commit/push。

交接时 `git status --short --branch` 的关键结果：

```text
## codex/wp-01-desktop-foundation
 M .gitignore
 M web/package-lock.json
 M web/package.json
?? .agents/
?? .codex/
?? .comet/
?? AGENTS.md
?? PRD.md
?? app.go
?? app_test.go
?? docs/
?? go.mod
?? go.sum
?? internal/
?? main.go
?? scripts/verify.ps1
?? scripts/verify.sh
?? wails.json
?? web/dist/
```

上表省略了若干根目录规格/提示词文件名，仅用于说明状态；完整文件清单以新会话重新执行的 Git 命令为准。

## 3. 文档事实漂移

以下文件不能被当成当前执行状态的唯一事实：

1. `docs/implementation/STATUS.md` 仍写着 “PLAN READY, IMPLEMENTATION NOT STARTED”，已经落后于实际工作树。WP-01 Task 11 必须按真实证据更新。
2. `docs/implementation/TRACEABILITY.md` 中部分 FR 编号语义沿用了早期映射，例如把 FR-020 写成 SQLite；当前 `PRD.md` 中 FR-020 是“项目、原著与章节导入”。后续更新追踪矩阵时必须以 PRD 标题为准重新对齐，不可机械修改状态列。
3. WP-01 计划 Task 10 写 Windows CI 安装 Go 1.24，但 Wails v2.15.0 的上游 `go.mod` 实际要求 Go 1.25.0；CI、README、基线和最终 STATUS 必须记录并使用 Go 1.25，除非先正式更改 Wails 决策。

## 4. 已完成进度

### WP-00 — COMPLETE

已完成仓库审计、基线、追踪矩阵、Wails ADR、SQLite 候选 ADR、Windows/POSIX 验证脚本；未修改产品代码。详见：

- `docs/implementation/BASELINE.md`
- `docs/implementation/REPO_AUDIT.md`
- `docs/implementation/TRACEABILITY.md`
- `docs/adr/0001-desktop-framework.md`
- `docs/adr/0002-sqlite-driver-and-migrations.md`
- `docs/implementation/STATUS.md`

### WP-01 Task 1 — COMPLETE / APPROVED

目标：恢复可移植、可复现的 npm 基线。

- 从 `web/package.json` 删除四个不应作为直接依赖的 Windows 原生包。
- 修复 `web/package-lock.json` 的可选平台依赖矩阵。
- 语义审计：新增 72 个 optional 平台记录，删除 0，版本/哈希变化 0。
- 独立验证：`npm ci --legacy-peer-deps` PASS（1,308 packages）；`npm run typecheck` PASS；`npm run build` PASS；`git diff --check` PASS。
- 保留既有动态导入与约 2.53 MB chunk 警告；未顺手升级依赖或修复审计告警。

### WP-01 Task 2 — COMPLETE / APPROVED

目标：为现有 React 前端增加最小 Wails v2 桌面壳。

- 新增 `go.mod`、`go.sum`、`main.go`、`app.go`、`app_test.go`、`wails.json`、`internal/buildinfo/version.go`、`web/dist/.gitkeep`。
- Go module：`github.com/newyngwieslash-ops/infinite-atelier-mine-0903`。
- Wails：v2.15.0；Go directive：1.25.0。
- 生产前端通过 `//go:embed all:web/dist` 与 `assetserver.Options` 嵌入，不启动额外 HTTP/Vite server。
- 1280×800 有框窗口；固定 UUID 单实例锁；第二实例数据全部忽略，只恢复、显示、聚焦已有窗口。
- 当前没有 Bind、Repository、数据库、Health 或 Provider 暴露。
- 修复生命周期锁边界：锁内只复制/清理 context，所有 Wails/OS/shutdown I/O 在锁外执行。
- 生命周期测试覆盖：关闭开始即使 context 不可见、窗口动作不持 app mutex、nil context no-op。
- `wailsjsdir` 实际配置为 `web/src`；Wails 会自动追加 `wailsjs`，最终生成目录才是 `web/src/wailsjs`。
- 独立验证：Go test/vet、Wails doctor、dry-run、真实 Windows production shell build、前端 typecheck/build 均 PASS；验证生成物已清理。

### WP-01 Task 3 — PARTIAL / CHANGES REQUESTED

已实现：

- `internal/domain/apperror`：稳定安全错误、Cause unwrap、16 随机字节的 32 位十六进制 diagnostic ID。
- `internal/infrastructure/appdirs`：`Root`、`Database`、`Files`、`Temp`、`Logs`、`Snapshots`；默认 `os.UserConfigDir()/InfiniteAtelier`；目录请求 0700；数据库固定为 `app.db`。
- `internal/infrastructure/logging`：JSON `slog`、`logs/app.jsonl`、0600 append、字段/消息/Header/Bearer/Query/assignment/嵌套结构脱敏、循环保护与幂等处理。
- `main.go`/`app.go`：在 `wails.Run` 前创建目录和 logger；普通资源先关闭，文件 logger 最后关闭；logger Close 失败走独立安全 stderr reporter；兜底清理使用 5 秒非 nil context。
- 大量 TDD 回归测试已覆盖大小写、vendor key、结构体/map/slice、LogValuer、转义引号、quoted key、Unicode/hex/octal、畸形 query、Header 与重复 sanitize。
- 最近验证：`go test ./...` PASS；`go vet ./...` PASS；`go build ./...` PASS；日志重点回归多轮 PASS；`git diff --check` PASS。

尚未关闭的唯一已知 Important：

- 问题类别：普通非敏感引号字段内部嵌套的敏感赋值漏洞。
- `internal/infrastructure/logging/logging.go` 的 assignment 扫描会跳过“非敏感外层键”的完整引号值，因此引号内部的敏感赋值不会被继续扫描。
- 已知失败形态：
  - `note="token=<fixture>"`
  - `{"message":"token=<fixture>"}`
  - `detail='master_password=<fixture>'`
- 原因：外层 `note`/`message`/`detail` 被判断为普通键后扫描器直接跳到引号末尾，内部凭据赋值未再进入脱敏流程。
- 质量审查结论：Changes requested。不得把 Task 3 标为 COMPLETE。
- 推荐最小修复：先写上述失败测试；在普通引号值内部继续安全扫描，或当值含明确敏感标记但无法可靠解析时整值 fail closed。避免继续扩大约 620 行的手写准解析器。
- 修复后必须重新通过独立规格审查和独立质量审查。

## 5. 当前验证与环境事实

| 项目 | 事实 |
|---|---|
| OS/Arch | Windows / amd64 |
| Node | v24.13.0 |
| npm | 11.6.2 |
| Go effective toolchain | go1.25.0，`GOTOOLCHAIN=auto` |
| 系统原 Go 基线 | 曾为 1.24.1；Wails v2.15 会触发 Go 1.25 toolchain 下载 |
| Wails module/CLI | v2.15.0；CLI 已安装在 GOPATH/bin，但当前 shell 的 PATH 中 `wails` 不可直接解析 |
| `go test ./...` | 最近 PASS |
| `go vet ./...` | 最近 PASS |
| `go build ./...` | 最近 PASS；存在 Go telemetry/stat-cache 主机权限警告但退出码 0 |
| `go test -race ./...` | 环境阻断：`C:\MinGW\bin\gcc.exe` 为 32 位，`cc1.exe: 64-bit mode not compiled in` |
| frontend typecheck/build | 最近 PASS；保留既有动态导入和大 chunk 警告 |
| Wails production build | Task 2 期间真实 PASS，生成的 exe/bindings 已核验后清理 |
| Format baseline | WP-00 时 `npm run format:check` 对 147 个既有文件 FAIL，未做无关大规模格式化 |

新会话应优先用绝对 CLI 路径或把 `$(go env GOPATH)/bin` 临时加入当前进程 PATH，不要假定 `wails` 已全局可解析。

## 6. WP-01 剩余完整任务清单

严格串行执行；每个 Task 使用 implementer → spec reviewer → quality reviewer，任何审查问题先回到同一 Task 修复。

WP-01 对应 PRD 的 FR-010 桌面运行时与 Go Core，并为 FR-160 素材存储/血缘、FR-180 日志/诊断/隐私以及 NFR-002/003/004/005 建立底座；关键验收为 AC-FOUND-001/002/003。

### Task 3R — 关闭日志嵌套赋值漏洞

1. 先补失败测试：普通外层 quoted value 中包含 `token=...`、JSON message 中包含敏感 assignment、单引号 detail 中包含 password assignment。
2. 做最小、线性、无 ReDoS 的修复；优先复用已有敏感名称判定与 sanitizer，避免新增另一套规则。
3. 保留普通 quoted 文本、结构化 JSON 相邻字段和多次 sanitize 幂等。
4. 跑 logging 定向测试多轮、`go test ./...`、`go vet ./...`、`go build ./...`、gofmt、`git diff --check`、Secret/生成物检查。
5. 重新做独立规格审查和质量审查；全部批准后才完成 Task 3。

### Task 4 — 证明并接受一个 SQLite 驱动

1. 只加入 `modernc.org/sqlite@v1.58.0`，不得并存 ORM 或第二驱动。
2. 实现 DSN/open helper；路径含空格；验证 `foreign_keys=1`、WAL、busy timeout 5000、连接池上限和外键拒绝。
3. 验证普通、`CGO_ENABLED=0`、race（环境可用时）及 Windows/Linux/Darwin 临时交叉编译；二进制不得入库。
4. 任一必需门禁失败才在临时隔离现场评估 `mattn/go-sqlite3@v1.14.49`；最终不得保留两个驱动。
5. 只有证据完整才把 ADR-0002 改为 Accepted；否则保持 Proposed 并令 WP-01 PARTIAL。
6. 新建 `THIRD_PARTY_NOTICES.md`，记录 Wails、modernc SQLite、bundled SQLite 及 `go list -m all` 需要声明的许可证。

### Task 5 — Migration、pre-migration snapshot 与 safe mode

1. 只建 foundation migration：`schema_migrations`、`file_objects`、`file_references`。
2. 用 embed、数字顺序、SHA-256 checksum、单 migration 单事务与 `user_version` 实现 runner。
3. 测 fresh apply、幂等、checksum mismatch、坏 migration rollback、重复/乱序版本。
4. 旧非空数据库升级前用 SQLite `VACUUM INTO` 建一致快照；路径必须由 `SELECT quote(?)` 安全生成并 fsync。
5. snapshot/migration 失败时关闭 writable handle，返回 ModeSafe 和稳定 AppError；不得自动删除、绕过或静默恢复。
6. 把数据库生命周期接入 app 关闭顺序；验证 WAL、foreign keys、close/reopen、快照可读。

### Task 6 — Content-addressed FileStore

1. 在 application/files 定义消费方 Store/Repository 接口与 Import service。
2. 临时文件流式写入、前 512 字节 MIME magic、SHA-256、Sync、分片目录、原子 rename、重复内容去重。
3. display filename 只校验不作为路径；拒绝 traversal、绝对路径、分隔符、colon/device、NUL、空名。
4. 取消/reader 失败必须清理 owned temp；Open 只接受 64 位小写 hex key，不返回任意绝对路径。
5. SQLite 参数化 upsert metadata；冲突 hash/storage/size fail closed；测试缺失 object 的外键拒绝。

### Task 7 — Health service、Wails binding 与 event envelope

1. Health Snapshot 固定字段：version、database、dataDirectory、safeMode、diagnostic。
2. Health service 通过小 Probe 接口做 PingContext；ready/safe_mode 均有测试。
3. HealthBinding 只暴露 Get；不返回 DB 路径、SQL、Cause、Secret 或 mutation 方法。
4. Event Envelope v1：crypto/rand ID、UTC RFC3339Nano、固定事件名 `core:event`。
5. DOM ready 发 `health.changed`。
6. 运行 `wails generate module`，检查 `web/src/wailsjs` 仅含预期 health 方法/model/runtime；生成文件不可手改。

### Task 8 — React health 接线并保留浏览器模式

1. 新建 runtime-safe desktop adapter；没有 Wails globals 时返回 null/no-op。
2. 用现有 React Query，key 为 `['desktop-health']`；正确订阅与清理 `core:event`。
3. 在现有 UserStatusActions 增加可访问的文本+图标状态：ready、safe mode、loading；不得只靠颜色。
4. 文案写入 en-US/zh-CN i18n；safe mode tooltip 含 diagnostic。
5. Browser/Vite 模式不得报 Wails global 异常或显示错误 badge，不把 health 放进 Zustand。

### Task 9 — Production desktop 与现有路由兼容证明

1. 跑 Go、race/环境记录、vet、verify 全门禁。
2. `wails build -clean`，确认 exe 内嵌前端且无需外部 Vite。
3. Windows smoke：单窗口、health ready、`/`、`/assets`、`/canvas`、`/director`、`/config`、第二实例聚焦、最终窗口关闭退出、重启 DB 版本不变、日志无敏感内容。
4. 只在 `t.TempDir()` 注入坏 migration 验证 safe mode；不得破坏真实 app DB。
5. 检查 tracked/ignored 产物、数据库、日志、媒体和 Secret。

### Task 10 — Verification、CI 与 README

1. `verify.ps1`/`verify.sh` 在存在 go.mod 时执行 Go test/vet；Wails 不可用时明确 SKIP，不掩盖本地 production build 证据。
2. Ubuntu web job 保持 repaired `npm ci`。
3. Windows job 使用 Go 1.25（纠正计划中的 1.24）、Node 22 或经基线批准的版本、Wails v2.15.0，跑 npm ci、Go test/vet、Wails build。
4. 未获批准前不虚构 macOS/Linux desktop packaging 覆盖。
5. README 记录浏览器/桌面命令、Wails 安装、数据目录、verify，以及 Provider/Secret 不在 WP-01。

### Task 11 — 用证据关闭 WP-01

1. 跑最终 Git、Go、race/skip、vet、verify、Wails clean build；记录版本、结果、warning、binary 路径/大小和手工 smoke。
2. 严格评估 AC-FOUND-001/002/003；任何子项缺失则 WP-01 为 PARTIAL/BLOCKED。
3. 更新 STATUS、TRACEABILITY、ADR-0002；修复本文第 3 节记录的状态/FR 编号/Go 版本漂移。
4. 确认无 key、DB、log、media、temp、binary、build output 被跟踪；released migration 未被改写；browser data 未碰；无真实 Provider 调用。
5. 按 AGENTS.md 固定格式报告 WP-01，推荐 WP-02 但停止等待明确批准。

## 7. WP-02 至 WP-12 完整任务清单与 PRD 映射

以下均为未来工作，不代表已实现。FR 映射以当前 `PRD.md` 标题为准。

### WP-02 — Secret、网络安全与 Provider Gateway 基础

- 依赖：WP-01。
- PRD：FR-140 Provider Gateway、FR-180 设置/日志/诊断/隐私、FR-170 敏感备份边界、NFR-003。
- 任务：SecretStore port 与 fake/native；Provider/Model/Capability；前端只读 Secret 状态/掩码；受控 HTTP client（URL、DNS/IP、重定向、TLS、timeout、body size）；Provider error taxonomy；OpenAI-compatible Text Generate/Stream；health check；请求脱敏审计；Mock provider 契约测试；旧 API key/脚本迁移警告和 feature flag；CI 动态代码/Secret 扫描。
- 验收：AC-FOUND-004、AC-FOUND-005 文本/安全子集、AC-SEC-001；前端不可读取 Secret；普通日志无测试密钥；SSRF corpus 全过。

### WP-03 — Persistent Job Manager 与多模态 Provider

- 依赖：WP-02。
- PRD：FR-150 Jobs、FR-140 Provider、FR-160 文件/血缘、FR-080 媒体子集、NFR-002/可靠性。
- 任务：Job/Attempt/ProviderRequest 表与状态机；scheduler/worker pool/lease/revision/retry；启动 recovery；OpenAI image、Gemini text/image、async video、audio/TTS contracts；URL/Base64/binary 统一进 FileStore；临时写/校验/原子提交；Job Center；批量、暂停、取消、仅重试失败；前端 Go adapter；production flag 禁止新增 browser-direct Provider。
- 验收：AC-FOUND-006、AC-MEDIA-001 Mock；重启恢复、幂等、失败不造成功资产、大文件不进 SQLite、至少一条图像生成走 Go。

### WP-04 — Legacy 项目迁移与画布后端适配

- 依赖：WP-03。
- PRD：FR-001 兼容、FR-130 Canvas、FR-160 File lineage、FR-170 Backup/Migration、NFR-004。
- 任务：识别旧格式；迁移 manifest；Project/Canvas/Asset/File；legacy ID mapping；迁移前快照；转换 node/edge/viewport/chat/history/media；保留 unsupported metadata；幂等导入；画布改走 Go repository adapter；Zustand 仅 UI；自由画布 E2E；旧数据只读策略；无密钥普通备份 v1。
- 验收：AC-LEGACY-001/002/003、AC-CANVAS-004、AC-BACKUP-001 基础；数量/哈希一致；失败不破坏旧数据。

### WP-05 — 短剧领域模型与 Studio UI Shell

- 依赖：WP-04。
- PRD：FR-020/030/040/050/060/070/100/110/130 的领域基础。
- 任务：Project settings/rules/style；Source/Chapter；StoryEntity/Event/Relation/Conflict；Episode/Skeleton/Strategy/Script/Scene/Dialogue/Shot；Asset/Version/Usage/Lineage；DirectorPlan/Storyboard/Panel；Workflow/Stage/Review/UserGate；Canvas entity refs/relation registry；Studio 导航/空状态；创建向导；领域 command/query/event；revision/version/approve/stale 测试；schema migration。
- 验收：MVP domain constraints、approved 唯一、locked rule、stale 传播、可创建 Drama Project/Episode/Asset、AC-CANVAS-001/002 子集。

### WP-06 — 原始文档、章节与事件图谱

- 依赖：WP-05。
- PRD：FR-020 项目/原著/章节、FR-030 事件图谱、NFR-002/003。
- 任务：TXT/Markdown/DOCX；编码/规范化；章节检测/调整/确认；原文 offset；大文本分块；EventExtraction schema 与 Mock service；entity/event candidate；接受/拒绝/修改/锁定；alias/conflict；Story Graph UI；Prompt Injection 隔离；恶意 DOCX/大文件测试。
- 验收：AC-STORY-001/002；原文定位；100k 中文字符不阻塞 UI；文档内容不可执行指令或建立外部关系。

### WP-07 — Agent Runtime、Skill、Workflow 与 Quality Gate

- 依赖：WP-05，建议 WP-06 完成。
- PRD：FR-090 Agent 中心、FR-100 Workflow、FR-110 Supervisor、FR-120 Memory port、Agent Runtime 章节。
- 任务：Agent registry/spec；Skill manifest/loader/version；Tool registry/authorizer；Decision/Execution/Supervisor runners；JSON Schema + 一次 repair；AgentRun/Message/ToolCall；Workflow state machine；StageRun/Review/UserGate；最大工具/时间/retry；cancel；deterministic Mock LLM；Agent Center/Quality Gate UI；Recent Memory port；空 Script/Production skill skeleton。
- 验收：AC-AGENT-001..005；Decision→Execution→Supervisor→User Gate canary；DB workflow state；Supervisor read-only；CI 不调用真实模型。

### WP-08 — ScriptAgent：骨架、策略与剧本

- 依赖：WP-06、WP-07。
- PRD：FR-040 ScriptAgent，兼及 FR-100/110。
- 任务：Script Decision Skill；Skeleton execution/schema/tools/supervisor；Adaptation Strategy execution/schema/tools/supervisor；Script generation execution/schema/tools/supervisor；PASS/FIX/REDO/MANUAL_EDIT；version diff；locked fields；Scene/Dialogue/Shot 持久化；Script UI；Canvas projection；模型策略；到 approved Script 的 Canary E2E。
- 验收：AC-SCRIPT-001/002/003；三层独立；原著事实来源；FIX 不改锁定内容；时长/结构完整；版本可追踪。

### WP-09 — ProductionAgent：资产、导演与分镜

- 依赖：WP-08；Provider/Job 来自 WP-03。
- PRD：FR-050 Asset Bible、FR-060 Director/MONOFORM、FR-070 Storyboard、FR-130 Canvas。
- 任务：Production Decision Skill；Director Plan Agent/schema/version；asset gap；创建/导入/候选资产；approved asset/usage/lineage；Storyboard Table/Panel agents 与 supervisors；批量 image jobs；candidate/approve/redo；Shot/Asset/Panel 投影；MONOFORM typed bridge；影响分析；到 approved storyboard images 的 E2E。
- 验收：AC-ASSET-001/002、AC-BOARD-001/002/003；缺资产阻断批量；单 Shot 重做不影响其他；全链路可追溯。

### WP-10 — Persistent Memory、Consistency 与 Quality Center

- 依赖：WP-07、WP-08、WP-09。
- PRD：FR-110 Supervisor/Quality、FR-120 Persistent Memory。
- 任务：MemoryItem/SummarySource/EntityLink；scope resolver；Recent/Summary；Float32 embedding；VectorIndex；threshold/topK/fusion；self-hit 排除；Deep Recall；Memory Inspector；Episodic/Semantic/Procedural/Artifact；与 Event Graph 分离；deterministic consistency；角色/服装/道具/场景连续性；Quality Center；Memory/Consistency E2E；Embedding ADR。
- 验收：AC-MEM-001..005、AC-E2E-004/005；跨项目泄漏 0；Supervisor evidence；记忆可查看/删除；低相关候选不强制返回。

### WP-11 — 视频、音频、字幕、时间线与导出

- 依赖：WP-09、WP-10。
- PRD：FR-080 Video/Audio/Subtitle/Export。
- 任务：Video job UI/provider；首尾帧/参考资产；Shot Video Version；TTS voice/dialogue mapping；narration/audio assets；SRT/VTT；基础 timeline；MediaEngine/FFmpeg ADR；安全参数化媒体处理；MP4；Script/Storyboard/Subtitle/Manifest export；Final Supervisor；中断恢复；单集 E2E。
- 验收：AC-MEDIA-001/002/003；Final Review；可播放；无 shell injection；导出清单可追溯。

### WP-12 — 硬化、性能、打包与 Release Candidate

- 依赖：WP-01..WP-11。
- PRD：FR-001、FR-170、FR-180、NFR-001..006、发布阻断条件和全部 MVP E2E。
- 任务：完整 verify；Unit/Integration/E2E/Security；1,000 node/2,000 edge；10k asset/memory benchmark；backup/restore 原子性；经 ADR 批准的加密敏感备份；上一版本 DB migration；Windows clean VM；installer/signing；notices/SBOM；Secret/vulnerability scan；Recovery/Safe Mode；用户文档；release checklist；危险 legacy path 删除或不可达；最终 E2E。
- 验收：所有发布阻断清零；Windows 安装/升级/恢复；普通备份无 Secret；动态脚本不可达；SSRF corpus；旧画布回归；未解决项如实记录。

## 8. PRD 发布阻断条件

以下任一项存在都不得发布：

1. 前端或普通备份能获得完整 API Key。
2. 仍有任意模型 JavaScript 执行路径。
3. API 代理可访问回环、私网或任意地址。
4. Workflow 状态只存在内存。
5. 视频结果未验证即标成功。
6. DB migration 无备份或回滚/修复路径。
7. Supervisor 可调用未授权写工具。
8. 导入可 path traversal 或 Zip Bomb。
9. 旧项目迁移静默丢失。
10. 核心 E2E 未通过。
11. License/third-party notices 缺失。

最终 MVP 还必须覆盖 AC-E2E-001 旧项目兼容、002 小说到分镜、003 中断恢复、004 质量修订、005 Deep Recall、006 安全。

## 9. 新 Codex 会话可直接提交的完整提示词

复制以下整段作为新会话首条消息：

```text
你正在以下现有仓库继续开发：
F:\AI_Movie_Things_202606\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\infinite-atelier-mine-0903

请使用已安装的 Ponytail 完整工作流，并明确授权你使用子代理，按“implementer → spec reviewer → quality reviewer”串行执行与复审。不要并行实现多个任务。

当前目标：继续执行已获用户批准的 WP-01，仅从 Task 3 的未关闭质量问题恢复；Task 3 获得规格和质量最终批准后，按现有计划串行执行 Task 4–11，完成 WP-01 后停止并按 AGENTS.md 格式报告。不要自动进入 WP-02。

开始任何修改前必须：
1. 完整按顺序阅读 PRD.md、AGENTS.md、docs/implementation/STATUS.md、docs/ROADMAP.md 当前 WP-01、docs/ARCHITECTURE.md、docs/DOMAIN_MODEL.md、docs/AGENT_CONTRACTS.md、docs/SECURITY.md、docs/ACCEPTANCE.md、docs/reference/INTEGRATION_ANALYSIS.md、docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md，以及 README/package/config/CI/相关代码和测试。
2. 完整阅读 docs/implementation/handoff-2026-09-05-163956+0800.md 与 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md。
3. 运行 git status --short --branch、git diff --cached --name-status、git diff --name-status、git diff --check，记录并保护全部现有 tracked/untracked 修改。
4. 不要相信 STATUS.md 中“WP-01 implementation not started”的过时文字；以 handoff、实际 Git 和审查结论为准。不得删除、覆盖、stash 或 reset 现有现场。

当前已确认事实：
- 分支 codex/wp-01-desktop-foundation；HEAD a243891455ec17687dd54b5ac90d3bd64478a1a1；没有 WP-01 commit。
- Task 1 npm lock repair 已完成并通过独立规格/质量审查。
- Task 2 Wails v2.15.0 最小桌面壳已完成并通过独立规格/质量审查；Go module 要求 Go 1.25，GOTOOLCHAIN=auto；wailsjsdir=web/src 是正确配置。
- Task 3 的 AppError、appdirs、结构化脱敏日志及生命周期接线主体已实现，规格审查通过，但最终质量审查为 Changes requested，尚未完成。
- 当前唯一已知 Important：logging assignment scanner 会跳过普通外层 quoted value 内部的敏感赋值。必须先用 TDD 复现并修复：note="token=<fixture>"、{"message":"token=<fixture>"}、detail='master_password=<fixture>'。普通 quoted 文本、相邻 JSON 字段、多次 sanitize 幂等和线性复杂度不得回归。优先在普通引号内容中复用现有 sanitizer，或对含明确敏感标记但无法可靠解析的歧义值整值 fail closed；不要继续扩张为新的通用 parser。
- 修复后先跑 logging 定向测试多轮、go test ./...、go vet ./...、go build ./...、gofmt、git diff --check、Secret/生成物检查；go test -race 当前会因 32 位 MinGW 无法编译 amd64 CGO 失败，必须记录为环境限制而非伪装通过。
- 随后必须由独立 spec reviewer 和 quality reviewer 重新审查；两者均批准后才算 Task 3 完成。

Git/数据安全：
- 不执行 commit/push/stash/reset/clean，除非我另行明确要求。
- 不覆盖用户文件，不修改真实数据库、密钥、浏览器数据或媒体。
- 不访问真实 Provider。
- 一次只做一个 Task；任何审查问题必须先在当前 Task 修复并复审。
- 不引入 ORM、第二 SQLite 驱动、Provider/Secret/Job/Agent/Drama 等超出 WP-01 的代码。
- Toonflow 仅 clean-room 参考，禁止复制其源码、Prompt、Skill、品牌、图标或识别性实现。

Task 3 最终批准后，严格按计划继续：
- Task 4：modernc SQLite v1.58.0 驱动矩阵、ADR-0002、THIRD_PARTY_NOTICES。
- Task 5：foundation migration、checksum、transaction、pre-migration snapshot、safe mode。
- Task 6：content-addressed FileStore、SQLite metadata、traversal/cancel/failure tests。
- Task 7：Health service/binding、core:event envelope、生成并审计 Wails bindings。
- Task 8：React Query health adapter/status、browser mode 安全回退、i18n/a11y。
- Task 9：Windows production build/smoke、路由/单实例/重启/safe-mode/Secret 证明。
- Task 10：verify/CI/README；CI Go 版本必须纠正为 1.25，因为 Wails v2.15 硬性要求 Go 1.25。
- Task 11：最终 AC-FOUND-001/002/003、STATUS/TRACEABILITY/ADR 更新和安全检查；TRACEABILITY 的 FR 编号必须按当前 PRD 标题重新校正。

请先用一条简短 commentary 告诉我你已读取 handoff 并准备恢复 Task 3，然后直接开始，不要再次询问是否继续。持续工作到 WP-01 完成或出现真正需要用户决策的阻塞；完成后停止，推荐 WP-02 但等待明确批准。
```

## 10. 交接后的第一组验证命令

新会话开始后建议先执行：

```powershell
git status --short --branch
git diff --cached --name-status
git diff --name-status
git diff --check
go test ./internal/infrastructure/logging -count=1 -v
go test ./... -count=1
go vet ./...
go build ./...
Set-Location web
npm run typecheck
npm run build
```

不要在尚未理解当前 untracked 现场前运行会删除、覆盖、重写 lockfile 或生成大量绑定/构建产物的命令。

## 11. Comet Native 交接记录

- Workflow：Native。
- Change：`wp01-session-handoff-20260905`。
- Workspace isolation：`current`。
- Change/target branch：`codex/wp-01-desktop-foundation`。
- 本次 Comet change 只负责生成和验证交接工件，不负责修复 Task 3 或继续产品实现。

## 12. 完成边界

本交接文档只说明当前真实状态。Task 3 仍为 PARTIAL，WP-01 仍为 IN PROGRESS。新会话在 WP-01 的所有验收和文档更新真实完成前，不得将 WP-01 标为 COMPLETE。
