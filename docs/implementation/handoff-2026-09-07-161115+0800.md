# Infinite Atelier Drama Studio 开发交接

> 交接时间（Asia/Shanghai）：2026-09-07T16:11:15+08:00  
> 仓库：`F:\AI_Movie_Things_202606\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\infinite-atelier-mine-0903`  
> 分支：`codex/wp-01-desktop-foundation`  
> HEAD：`a243891455ec17687dd54b5ac90d3bd64478a1a1`  
> 当前工作包：WP-01 — Wails v2、Go Core、SQLite 与文件底座  
> 精确恢复点：Task 4 implementer 中途现场；先完成 Task 4 矩阵，再做独立 spec/quality 双审

## 1. 一句话交接

WP-00 已完成；WP-01 Task 1、Task 2、Task 3 均已实现并通过独立规格与质量审查。Task 4 已写入 modernc SQLite v1.58.0 驱动、定向测试、ADR 草稿、Go 依赖与第三方 notices，Windows/amd64 定向测试通过，但依赖整理结果、CGO0、三目标交叉编译、race 环境记录、完整验证以及独立规格/质量审查尚未完成。因此 Task 4 与 WP-01 都是 IN PROGRESS。新会话必须从 Task 4 继续，双审批准前不得进入 Task 5；完成 WP-01 后停止，不得自动进入 WP-02。

## 2. Git 与数据安全现场

- 分支与 HEAD 保持上述值；没有 WP-01 commit。
- staged diff 为空。
- tracked 修改仍为 `.gitignore`、`web/package.json`、`web/package-lock.json`。
- 大量 WP-00/WP-01、规范、Comet 和根目录文件为 untracked；untracked 不表示可删除。
- 新增的 Task 4 文件包括 `THIRD_PARTY_NOTICES.md`、`internal/infrastructure/database/driver.go`、`driver_test.go`；`go.mod`、`go.sum` 和 `docs/adr/0002-sqlite-driver-and-migrations.md` 已发生未提交变化。
- 本次 handoff 还新增 `docs/comet/changes/wp-01-session-handoff/` 和本文件。
- 未执行 commit、push、stash、reset、clean；未改动真实数据库、浏览器资料、密钥、媒体或 Provider；未调用真实付费 Provider。
- Git 读取会警告无法访问宿主全局 ignore，并提示部分 LF 将转换为 CRLF；这不是产品失败，不应通过覆盖用户 Git 配置处理。
- 任何新会话必须先重新运行四个 Git 命令，不得仅依赖本文摘要。

交接时关键状态：

```text
## codex/wp-01-desktop-foundation
 M .gitignore
 M web/package-lock.json
 M web/package.json
?? .agents/
?? .codex/
?? .comet/
?? AGENTS.md
?? PRD.md
?? THIRD_PARTY_NOTICES.md
?? app.go
?? app_test.go
?? docs/
?? go.mod
?? go.sum
?? internal/
?? main.go
?? scripts/verify.ps1
?? scripts/verify.sh
?? wails.json
?? web/dist/
```

这只是压缩摘要；完整 tracked/untracked 清单以 `git status --short --branch` 为准。

## 3. 必读事实与文档漂移

新会话修改前必须完整按 AGENTS.md 顺序阅读：

1. `PRD.md`
2. `AGENTS.md`
3. `docs/implementation/STATUS.md`
4. `docs/ROADMAP.md` 当前 WP-01
5. `docs/ARCHITECTURE.md`
6. `docs/DOMAIN_MODEL.md`
7. `docs/AGENT_CONTRACTS.md`
8. `docs/SECURITY.md`
9. `docs/ACCEPTANCE.md`
10. `docs/reference/INTEGRATION_ANALYSIS.md`
11. `docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md`
12. README、package、配置、CI、相关代码和测试
13. `docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md`
14. `docs/implementation/handoff-2026-09-05-163956+0800.md`
15. `docs/implementation/wp01-execution-2026-09-07.md`
16. 本 handoff

必须保留以下纠正：

- `STATUS.md` 中 “WP-01 implementation not started” 已过时，以实际 Git、handoff 与审查证据为准。
- Wails v2.15.0 和 modernc SQLite v1.58.0 都要求 Go 1.25；计划中 Go 1.24 的示例已过时。
- `wailsjsdir=web/src` 是正确配置，Wails 会在其下追加 `wailsjs`。
- TRACEABILITY 必须按当前 PRD 标题重校 FR：FR-010 桌面、FR-020 项目/原著/章节、FR-160 素材存储、FR-180 设置/日志/诊断/隐私。
- Toonflow 仅 clean-room 思想参考，禁止复制源码、Prompt/Skill 原文、品牌、图标、界面文案、素材或识别性实现。

## 4. 已完成工作

### WP-00 — COMPLETE

仓库审计、基线、追踪矩阵、桌面与 SQLite ADR 初稿、Windows/POSIX verify 脚本已完成。WP-00 历史记录不得因后续 STATUS 更新而丢失。

### WP-01 Task 1 — APPROVED

- npm lock 修复完成。
- 已通过独立 spec reviewer 与 quality reviewer。
- 现有 `web/package.json` 与 `web/package-lock.json` 属于需保留的用户现场。

### WP-01 Task 2 — APPROVED

- Wails v2.15.0 最小桌面壳、Go 1.25 module、应用生命周期与单实例框架已落盘。
- `wailsjsdir=web/src` 已确认正确。
- 已通过独立 spec reviewer 与 quality reviewer。

### WP-01 Task 3 — APPROVED

- AppError、appdirs、结构化脱敏日志与生命周期接线完成。
- 已用 TDD 修复普通外层引号内敏感赋值遗漏：`note="token=<fixture>"`、JSON message、单引号 `master_password`，并覆盖嵌套异类引号、普通文本、相邻字段、五次幂等和线性复杂度。
- 定向 logging 测试多轮、`go test ./...`、`go vet ./...`、`go build ./...`、gofmt、`git diff --check` 均通过。
- 独立 spec reviewer 与 quality reviewer 均最终 APPROVED，无遗留 Critical/Important/Minor。
- `go test -race` 因本机 `C:\MinGW\bin\gcc.exe` 是 32 位工具链，无法编译 amd64 CGO，报 `cc1.exe: 64-bit mode not compiled in`。这是明确环境限制，不得记录为 PASS。

## 5. 当前中断点：WP-01 Task 4

### 已落盘但尚未获批准

- `internal/infrastructure/database/driver.go`
  - 仅使用 `database/sql` 与 `modernc.org/sqlite`。
  - `net/url` 构造 file URI，支持空格、Unicode、`# % & '` 等保留字符路径。
  - 每个连接启用 `foreign_keys(1)`、`journal_mode(WAL)`、`busy_timeout(5000)`。
  - 最大 open/idle connection 均为 1；取消等待能透传 context。
  - 打开失败返回安全的 `DATABASE_OPEN_FAILED` AppError，不泄漏路径。
- `internal/infrastructure/database/driver_test.go`
  - 覆盖特殊路径、替换连接 pragma、缺失父记录外键失败、连接池取消、打开失败、预取消不创建文件。
- `go.mod` / `go.sum`
  - 已出现 modernc.org/sqlite v1.58.0 及其传递依赖。
  - 当前 `go.mod` 仍把 modernc SQLite 列在 indirect block；此前 `go mod tidy` 的高权限执行被中断，没有可信完成结果，必须重跑并检查最终 direct/indirect 分类。
- `docs/adr/0002-sqlite-driver-and-migrations.md`
  - 已更新 Task 4 证据，但状态仍正确保持 Proposed。
  - 不得在迁移、快照、生产打包和剩余证据完成前改成 Accepted。
- `THIRD_PARTY_NOTICES.md`
  - 已生成较完整的 Go 桌面依赖 notices 草稿，包括 Wails、modernc、SQLite、libc/memory/sqlite-vec 等声明。
  - 必须由 Task 4 spec/quality reviewer 审查准确性与分发范围，不能把它当成最终 SBOM。

### 已验证

- implementer TDD 红→绿已发生：最初 `openDriver` 不存在，之后三项 driver tests 通过。
- 根代理独立运行：

```text
go test ./internal/infrastructure/database -count=1 -v
```

结果 PASS，SQLite 3.53.4；`TestDriverContract`、`TestDriverOpenFailure`、`TestDriverCanceledOpen` 均通过。
- 主机 Go 1.25.0，`GOTOOLCHAIN=auto`；Wails CLI 位于 `D:\GoWorks1.18\bin\wails.exe`。
- modernc SQLite v1.58.0 与 Wails v2.15.0 本地 license/go.mod 已检查；SQLite 为 public domain，modernc driver BSD-3-Clause，Wails MIT。

### 仍必须完成

1. 重新运行并确认 `go mod tidy`，检查只保留一个 SQLite driver、无 ORM，modernc driver 为直接依赖。
2. 运行定向测试多轮、`CGO_ENABLED=0` 定向测试并恢复调用者原环境变量。
3. `go test -c` 分别交叉编译 Windows、Linux、Darwin 测试包到临时目录，不提交二进制。
4. 运行 `go test ./...`、`go vet ./...`、`go build ./...`、gofmt、`git diff --check`。
5. 运行 race 并如实记录本机 MinGW 环境失败；不能因该环境限制引入 mattn 第二驱动，也不能伪造通过。
6. 检查 secret、数据库、日志、媒体、临时文件和生成物；清理只能针对本任务明确拥有的临时目录。
7. 完成 implementer 交付后，新建独立只读 spec reviewer；修复其问题并复审通过后，再新建独立只读 quality reviewer。
8. 两名 reviewer 均 APPROVED 后，Task 4 才能完成并进入 Task 5。

## 6. WP-01 剩余任务清单

### Task 5 — Foundation migration、snapshot 与 safe mode

- 只创建 `schema_migrations`、`file_objects`、`file_references` 三张基础表。
- 用 `//go:embed`、数字版本、SHA-256 checksum 和单 migration transaction 实现最小 runner。
- 覆盖 fresh、幂等、checksum mismatch、坏第二迁移回滚、数字排序、重复版本、rows close/error。
- 旧非空 DB 升级前用 `VACUUM INTO` 建快照；路径必须由 `SELECT quote(?)` 生成 SQL literal 并 fsync。
- snapshot/migration/integrity 失败时关闭 writable handle，返回 ModeSafe 与稳定诊断，不删除、自动恢复或绕过失败。
- 在 Wails 单实例锁获得之后初始化 DB，按正确顺序 checkpoint/close。

### Task 6 — Content-addressed FileStore

- 在 application/files 定义消费方 Store/Repository 接口与 Import service。
- 流式临时写入、前 512 字节 MIME magic、SHA-256、Sync、分片目录、原子 rename、重复内容去重。
- display filename 只校验不作为路径；拒绝 traversal、绝对路径、分隔符、colon/device、NUL、空名和符号链接逃逸。
- 取消或 reader 失败必须清理 owned temp；Open 仅接受 64 位小写 hex，不暴露绝对路径。
- SQLite 参数化 metadata upsert；hash/storage/size 冲突 fail closed；缺失 object 的 reference 外键失败。

### Task 7 — Health service、Wails binding 与 core:event

- Health Snapshot 固定字段：Version、Database、DataDirectory、SafeMode、Diagnostic。
- Health service 通过小 Probe 接口做 PingContext；ready 与 safe_mode 均有测试。
- HealthBinding 只暴露 Get，不暴露 setter、DB、SQL、Cause、Secret 或 repository。
- event envelope v1 使用随机 ID、UTC RFC3339Nano、固定 `core:event`；DOM ready 发 `health.changed`。
- 生成并审计 `web/src/wailsjs`，不可手改生成文件，只允许预期 health method/model/runtime。

### Task 8 — React Query health 与 browser fallback

- runtime-safe desktop adapter；没有 Wails globals 时返回 null/no-op。
- 复用 TanStack Query，订阅并清理 `core:event`，不把健康事实放进 Zustand。
- UserStatusActions 增加文本+图标的 ready/loading/safe mode，支持键盘、焦点和 screen reader；状态不得只靠颜色。
- en-US/zh-CN i18n；safe mode tooltip 含 diagnostic。
- Vite/browser 模式无 Wails global 异常、无错误 badge。

### Task 9 — Windows production build 与 smoke

- 运行 Go、vet、verify、race 环境记录；`wails build -clean` 生成内嵌前端 exe。
- 只用隔离临时 APPDATA/LOCALAPPDATA/WebView profile，不碰真实用户目录。
- 原生 smoke：单窗口、health ready、`/`、`/assets`、`/canvas`、`/director`、`/config`；第二实例聚焦第一实例；最终关闭退出；重启 DB version 不变；日志无敏感内容。
- safe mode 只通过测试临时目录注入坏 migration。
- 记录 exe 路径、大小、SHA-256、启动行为与警告；构建后恢复 `web/dist/.gitkeep`。

### Task 10 — verify、CI 与 README

- 修复/扩展 PowerShell 与 POSIX verify；POSIX 脚本不得停留在 web 目录后错误执行 Go。
- Go/Wails 不可用时明确 SKIP；失败必须非零，不掩盖本地 production evidence。
- 保留 Ubuntu web job；增加 Windows Go 1.25、Node 22、Wails v2.15.0，运行 npm ci、Go test/vet、Wails build。
- 未获证据前不宣称 macOS/Linux 桌面 packaging 已通过。
- README 记录 browser/desktop 命令、Go/Wails 前置条件、数据目录、verify、当前 Provider/Secret 边界。

### Task 11 — 证据关闭 WP-01

- 完整运行 Git、Go、race 环境记录、vet、verify、Wails clean build、Windows native smoke。
- 严格逐项评估 AC-FOUND-001/002/003；缺项则 PARTIAL/BLOCKED。
- 更新 STATUS、TRACEABILITY、ADR-0002；保留 WP-00 历史并校正 FR 标题与 Go 版本漂移。
- 检查无 key、DB、log、media、temp、binary、build output 被跟踪；released migration 未改写；浏览器数据未碰；无真实 Provider。
- 按 AGENTS.md 固定格式报告；推荐 WP-02，但停止等待明确批准。

## 7. 对标 PRD 的 WP-02–WP-12 完整后续清单

以下均未获本会话实施授权，也不代表已完成。

### WP-02 — Secret、网络安全与 Provider Gateway 基础

- 映射：FR-140、FR-170、FR-180、NFR-003；AC-FOUND-004/005、AC-SEC-001。
- SecretStore fake/native；Provider/Model/Capability；前端只读 Secret 状态。
- 受控 HTTP client：URL、DNS/IP、重定向、TLS、timeout、body limit、header allowlist、日志脱敏和 SSRF corpus。
- OpenAI-compatible Text Generate/Stream、health、error taxonomy、Mock provider contract。
- 隔离旧 API key/脚本迁移；前端不能读取完整 Secret。

### WP-03 — Persistent Job Manager 与多模态 Provider

- 映射：FR-140、FR-150、FR-160、FR-080 子集、NFR-002；AC-FOUND-006、AC-MEDIA-001 子集。
- Job/Attempt/ProviderRequest、状态机、scheduler/worker、lease/revision/retry、重启恢复、取消与幂等。
- image/video/audio/TTS contracts；URL/Base64/binary 统一进入 FileStore。
- Job Center、批量/暂停/取消/仅重试失败；CI 使用 deterministic Mock。

### WP-04 — Legacy 项目迁移与画布后端适配

- 映射：FR-001、FR-130、FR-160、FR-170、NFR-004；AC-LEGACY-001/002/003、AC-CANVAS-004、AC-BACKUP-001 基础。
- 识别旧格式、迁移 manifest、ID mapping、快照、node/edge/viewport/chat/history/media 转换。
- 保留 unsupported metadata、幂等导入；画布改走 Go repository adapter，Zustand 仅 UI。

### WP-05 — 短剧领域模型与 Studio UI Shell

- 映射：FR-020/030/040/050/060/070/100/110/130。
- Project/Source/Chapter、Story Graph、Episode/Script/Scene/Shot、Asset/Version/Usage、Director/Storyboard、Workflow/Review/UserGate 等领域模型与约束。
- Studio 导航、创建向导、领域 command/query/event、revision/approve/stale 测试。

### WP-06 — 原始文档、章节与事件图谱

- 映射：FR-020、FR-030、NFR-002/003；AC-STORY-001/002。
- TXT/Markdown/DOCX、编码归一化、章节确认、原文 offset、大文本分块。
- EventExtraction schema、candidate accept/reject/edit/lock、alias/conflict、Story Graph UI。
- Prompt Injection、恶意 DOCX、Zip Bomb 和大文件测试。

### WP-07 — Agent Runtime、Skill、Workflow 与 Quality Gate

- 映射：FR-090/100/110/120；AC-AGENT-001..005。
- Agent/Skill/Tool registry；Decision、Execution、Supervisor 三层独立 runner。
- Schema + 一次 repair、最大工具/时间/retry、cancel、审计、deterministic Mock LLM。
- Durable workflow、StageRun/Review/UserGate、Recent Memory port、Agent Center/Quality Gate UI。

### WP-08 — ScriptAgent：骨架、策略与剧本

- 映射：FR-040、FR-100、FR-110；AC-SCRIPT-001/002/003。
- Skeleton、Adaptation Strategy、Script generation 的 decision/execution/supervisor。
- PASS/FIX/REDO/MANUAL_EDIT、版本 diff、锁定字段、Scene/Dialogue/Shot 持久化、Canvas 投影。

### WP-09 — ProductionAgent：资产、导演与分镜

- 映射：FR-050/060/070/130；AC-ASSET-001/002、AC-BOARD-001/002/003。
- Production Decision、Director Plan、asset gap、approved asset/usage/lineage。
- Storyboard Table/Panel agents 与 supervisors；批量 image jobs；candidate/approve/redo；影响分析与追溯。

### WP-10 — Persistent Memory、Consistency 与 Quality Center

- 映射：FR-110、FR-120；AC-MEM-001..005、AC-E2E-004/005。
- MemoryItem/SummarySource/EntityLink、scope resolver、Recent/Summary、embedding/vector index、threshold/topK/fusion、self-hit 排除、Deep Recall。
- Episodic/Semantic/Procedural/Artifact 分类；与 Story Graph 分离；连续性检查与 Memory Inspector。

### WP-11 — 视频、音频、字幕、时间线与导出

- 映射：FR-080；AC-MEDIA-001/002/003。
- Video job、首尾帧/参考资产、Shot Video Version；TTS/dialogue/narration；SRT/VTT。
- 基础 timeline、MediaEngine/FFmpeg ADR、安全参数化媒体处理、MP4 与 manifest 导出、Final Supervisor。

### WP-12 — 硬化、性能、打包与 Release Candidate

- 映射：FR-001、FR-170、FR-180、NFR-001..006 与全部发布阻断条件。
- 完整 Unit/Integration/E2E/Security；1,000 node/2,000 edge；10k asset/memory benchmark。
- backup/restore、上一版本 DB migration、Windows clean VM、installer/signing、SBOM/notices、Secret/vulnerability scan、Recovery/Safe Mode、release checklist。

## 8. PRD 发布阻断条件

以下任一存在都不得发布：

1. 前端或普通备份能获得完整 API Key。
2. 仍有任意模型 JavaScript 执行路径。
3. API 代理可访问回环、私网或任意地址。
4. Workflow 状态只存在内存。
5. 视频结果未验证即标成功。
6. DB migration 无备份或回滚/修复路径。
7. Supervisor 可调用未授权写工具。
8. 导入可 path traversal 或 Zip Bomb。
9. 旧项目迁移静默丢失。
10. 核心 E2E 未通过。
11. License/third-party notices 缺失。

## 9. 新 Codex 或 Grok 会话完整提示词

复制以下整段作为新会话首条消息：

```text
你正在以下现有仓库继续开发：
F:\AI_Movie_Things_202606\Infinite_Atelier_Drama_Studio_Spec_Pack_v1.0\infinite-atelier-mine-0903

请使用已安装的 Ponytail 完整工作流。明确授权你使用子代理，但必须按单个任务串行执行：implementer → 独立 spec reviewer → 独立 quality reviewer。不要并行实现多个任务；当前任务的任何审查问题必须先修复并复审。若当前平台不支持真正独立的子代理/会话审查，必须停止并说明能力缺口，不得由 implementer 冒充独立 reviewer。

当前目标：继续执行已获批准的 WP-01，仅从 Task 4 的中途现场恢复。Task 4 获得规格和质量最终批准后，再按现有计划串行执行 Task 5–11。完成 WP-01 后停止，按 AGENTS.md 固定格式报告，推荐 WP-02 但等待明确批准；不要自动进入 WP-02。

开始任何修改前必须：
1. 完整按顺序阅读 PRD.md、AGENTS.md、docs/implementation/STATUS.md、docs/ROADMAP.md 当前 WP-01、docs/ARCHITECTURE.md、docs/DOMAIN_MODEL.md、docs/AGENT_CONTRACTS.md、docs/SECURITY.md、docs/ACCEPTANCE.md、docs/reference/INTEGRATION_ANALYSIS.md、docs/reference/TOONFLOW_AGENT_MEMORY_ANALYSIS.md，以及 README/package/config/CI/相关代码和测试。
2. 完整阅读 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md、docs/implementation/handoff-2026-09-05-163956+0800.md、docs/implementation/wp01-execution-2026-09-07.md，以及最新的 docs/implementation/handoff-2026-09-07-161115+0800.md。
3. 运行 git status --short --branch、git diff --cached --name-status、git diff --name-status、git diff --check，记录并保护全部现有 tracked/untracked 修改。
4. 不要相信 STATUS.md 中“WP-01 implementation not started”的过时文字；以最新 handoff、实际 Git、测试和审查结论为准。不得删除、覆盖、stash、reset 或 clean 现有现场。

当前确认事实：
- 分支 codex/wp-01-desktop-foundation；HEAD a243891455ec17687dd54b5ac90d3bd64478a1a1；没有 WP-01 commit。
- Task 1 npm lock repair 已完成并通过独立规格/质量审查。
- Task 2 Wails v2.15.0 最小桌面壳已完成并通过独立规格/质量审查；Go module 要求 Go 1.25，GOTOOLCHAIN=auto；wailsjsdir=web/src 正确。
- Task 3 AppError、appdirs、结构化脱敏日志、生命周期和普通引号敏感赋值修复已完成；独立 spec reviewer 与 quality reviewer 均最终 APPROVED。
- Task 4 仍为 IN PROGRESS：modernc.org/sqlite v1.58.0 驱动、driver tests、go.mod/go.sum 变化、ADR-0002 草稿与 THIRD_PARTY_NOTICES 草稿已落盘。根代理独立定向测试 PASS，SQLite 3.53.4。
- Task 4 尚未完成：此前高权限 go mod tidy 被中断，结果不可信；CGO_ENABLED=0、Windows/Linux/Darwin test cross-compile、完整 Go test/vet/build/gofmt/diff、race 环境记录和独立 spec/quality 双审都尚未完成。不得把 Task 4 标为完成。
- go test -race 预计会因 C:\MinGW\bin\gcc.exe 为 32 位、无法编译 amd64 CGO 而失败；必须记录为环境限制，不能伪装通过，也不能仅因此添加 mattn 第二驱动。

先完成 Task 4 implementer：
1. 核对现有 driver.go、driver_test.go、go.mod、go.sum、ADR-0002、THIRD_PARTY_NOTICES，保留正确内容，不从头重写。
2. 重新运行 go mod tidy 并检查 modernc.org/sqlite v1.58.0 是唯一直接 SQLite driver；不得引入 ORM、wrapper 或第二驱动。
3. 跑 driver tests 多轮、CGO_ENABLED=0，并恢复调用者原 CGO_ENABLED。
4. 用 go test -c 将 database test package 交叉编译到临时目录，目标 Windows/Linux/Darwin；不提交二进制。
5. 跑 go test ./...、go vet ./...、go build ./...、gofmt、git diff --check、Secret/生成物检查；race 如实记录环境失败。
6. ADR-0002 保持 Proposed，直到迁移、snapshot、production build 等证据满足；Task 4 只记录已验证事实。
7. implementer 完成后，依次启动独立只读 spec reviewer、独立只读 quality reviewer；任何发现先修复并重新审查，两者 APPROVED 后才进入 Task 5。

Task 5–11 严格按 docs/plans/2026-09-04-wp-01-secure-desktop-foundation.md：
- Task 5：foundation migration、checksum、transaction、pre-migration VACUUM INTO snapshot、safe mode、integrity/lifecycle。
- Task 6：content-addressed FileStore、SQLite metadata、traversal/symlink/cancel/failure/dedup tests。
- Task 7：Health service/binding、core:event envelope、生成并审计 Wails bindings。
- Task 8：React Query health adapter/status、browser mode 安全回退、i18n/a11y。
- Task 9：隔离用户目录的 Windows production build/native smoke、路由/单实例/重启/safe-mode/Secret 证明。
- Task 10：verify/CI/README；CI Go 版本必须为 1.25，保留真实 SKIP/FAIL 语义。
- Task 11：最终 AC-FOUND-001/002/003、STATUS/TRACEABILITY/ADR 更新与安全检查；TRACEABILITY 按当前 PRD 标题重校。

Git/数据安全：
- 不执行 commit/push/stash/reset/clean，除非用户另行明确要求。
- 不覆盖用户文件，不修改真实数据库、密钥、浏览器数据或媒体，不访问真实 Provider。
- 一次只做一个 Task；不得用未来 Task 的代码补当前验收。
- 不引入 ORM、第二 SQLite 驱动、Provider/Secret/Job/Agent/Drama 等超出当前 Task/WP-01 的代码。
- Toonflow 仅 clean-room 参考，禁止复制其源码、Prompt/Skill、品牌、图标、文案、素材或识别性实现。
- 构建可能删除空的 web/dist/.gitkeep；验证后必须恢复这个已知空占位文件，不能借机覆盖其他产物。

请先用一条简短 commentary 说明你已完整读取最新 handoff 并准备从 Task 4 恢复，然后直接开始。持续工作到 WP-01 完成或出现真正需要用户决策的阻塞；完成后停止，不要进入 WP-02。
```

## 10. 新会话第一组命令

```powershell
git status --short --branch
git diff --cached --name-status
git diff --name-status
git diff --check
go env GOVERSION GOTOOLCHAIN CGO_ENABLED GOOS GOARCH CC GOMODCACHE GOPATH
go mod tidy
go test ./internal/infrastructure/database -run TestDriver -count=20 -v
go test ./... -count=1
go vet ./...
go build ./...
```

`go mod tidy` 若因沙箱无法写 Go module cache，按宿主审批机制重跑；不要修改全局缓存权限或换用第二驱动绕过。

## 11. Comet Native 记录

- Workflow：Native。
- Change：`wp-01-session-handoff`。
- Workspace：current，绑定现有 `codex/wp-01-desktop-foundation`。
- 本 change 只生成与验证交接工件，不继续产品实现。
- 正式 Shape artifacts：`docs/comet/changes/wp-01-session-handoff/`。
- Archive 完成状态以 `comet-state.yaml` 与 `verification.md` 为准；本文生成时 Comet 尚处于 Build，不能提前声称已归档。

## 12. 完成边界

本 handoff 的完成只表示交接材料准确可复用。Task 4、Task 5–11 和 WP-01 仍未完成。新会话必须以测试和独立审查证据推进，不能因已有草稿、能编译或文档存在而标记 COMPLETE。
